一些待修正的问题：
1，统计，死链，去重视图，上方工具栏按钮，点击之后的highlight效果不会有。下次重新进入采用被选中的模式的按钮高亮
2，树视图的层次缩进对齐问题。之前优化了每一行书签icon和文本的空隙，现在导致文件夹下书签的icon左边缘，比文件夹文本的左边缘靠左，视觉上就有种没对齐的感觉。一个方案就是让子文件夹icon的左起点和父文件夹标题的左边对齐，但我不知道是否你有更优雅的方案。你可以截图验证，选一个严谨细致的解决方法。别忘了复用scripts下你现有的诊断对齐的脚本。
3，命令面板里面如果不是输入slach command，而是直接输入关键词进行搜索，这个搜索记录似乎不会进入到搜索视图的历史记录之中，请确认这个设计是否合理，要不要改进
4，搜索视图，上方区域控制了总条数，尽量控制上区不出现滚动条，不好看
5，现在又的slash command别名需要清理以下，太长或者不是语义很明确的不要放进去，不要为了追求多而多，比如/path控制路径显示隐藏反而让人困惑。比如/themedark明显改为 /theme dark更合理，你要综合考虑以后vBookmarks/docs/palette-commands-design.md的设计方案，做出统一，并提前做好保留指令清单
6，实现vBookmarks/docs/palette-commands-design.md中的方案并做好验收
7，去重视图的选择模式，点击后，按钮-去重所选（0 后面是乱码（中文语言下），检查。
8，死链和去重的选择模式，按空格有bug，对于死链视图，是翻页，对于去重视图是展开和收缩组。我觉得选择模式下空格的语义就是切换焦点所在元素的选中与不选中状态。请补充一致性设计并检查。同时考虑非选择模式下的语义。同时考虑当前各视图是否支持翻页的按键。
9, 去重视图的重复组按钮，仍然没有“应用”该组去重的快速按钮，右键菜单已经有了单独对该组操作的选项。
10，目前在edge里，Ctrl+1切换扩展的tab是失效的，默认是切换浏览器的tab页，请考虑默认快捷键冲突的情况，合理安排默认按键
11, 检查omnibar的本扩展搜索有没有问题
12, 评估是否能够在不大改的情况下，同时支持其他浏览器，重点是edge和firefox。可以的话提供面向它们的打包脚本或者同一个打包脚本的不同配置。
13, 死链检测优化了代理机制后，视图工具栏区域加了按钮，成了两行工具栏，现在方向键操作，焦点不会在第二行工具栏停留。你需要考虑如何做出优化。
14, 死链和去重都会引起书签的巨大变化，使用前都应该出现横幅告知对书签改动有风险，建议先进行备份。并附上chrome本身书签备份和导入的说明文档链接。用户可以选择警告信息不再提示，则之后不再显示（除非大版本更新，类似donation的逻辑）。这些条幅的出现，如果影响了之前的keyboard操作设计（类似#13），请注意同步修改。
15, 以上修改设计文档回填的，请确保回填，特别是面向用户的guide文档，以及readme里面的功能说明部分等。