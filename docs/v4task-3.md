一些待修正的问题：
1,最近添加和统计视图，横向的滚动条一直存在，影响观感，需要修正
2,统计视图，顶部按次数，按最近似乎没有作用，请确认点击之后是移动这两个区域的先后顺序还是如何？当前是最近访问（历史记录）靠前，而“按最近”也没高亮
3,宽度较宽的时候，死链视图每个条目是双行显示，然而右侧标记和删除的按钮还是靠顶部对齐，没有居中对齐
4,死链视图应该提供一个选择按钮，点击后进入选择模式，可以全选，反选，取消全部选择等，然后提供标记选择，取消标记选择等功能入口
5,去重视图同上，应该对重复组提供一个选择按钮，点击后进入选择模式，可以全选，反选，取消全部选择等，然后提供应用去重的功能入口；另外额外在每个重复组的标题栏右上应该提供一个单独的应用去重的快捷按钮，点击后可以直接对该组选择的情况直接应用去重，同步更新上方条幅的重复统计。
6,记忆视图位置，下次打开的时候停留在上次视图里，提供选项，默认是记忆。不记忆的话则是默认回到标签树视图。
7,之前提到的遗留项应该进行实施并尽量想办法通过自动化测试验收
- **§2.1 Tab/Shift+Tab 三区域循环**（Header→TabStrip→List，List 单 Tab 位 +  roving tabindex 行模型）：两分支共同缺口，规范已定但实现量大（document 级  Tab 拦截 + 行 tabindex 改 roving），建议单独立项并在真实 popup 里做焦点可用性验证。
- 区域焦点记忆（切视图恢复上次 `.focus` 行）：两边都只持久化了 scrollTop，  可随 Tab 循环一起做。
8,搜索栏右侧悬浮的手动点击清空按钮看不到，只能靠键盘进行清空
9,本次升级在弹出捐赠横幅的时候，醒目提示4.0的重大变化，并附上guide-v4的在线地址链接推荐阅读。
10,统计视图，最近访问里面，不在收藏夹的条目没有右键菜单，在收藏夹的条目有完整菜单，都不对。理论上这里都应该提供几种打开方式的菜单项，非收藏夹项目可提供收藏菜单
11,其他非树视图的书签菜单都需要进行优化，在不可目见树结构的地方，不应该提供和方位有关的选项（比如此前添加，此后添加等），只应该提供直观的选项
12,方向键上下在很多视图无法按照设计一样无缝转移焦点，经常会丢失和操作无效，需要专门测试检查验收。比如：焦点在顶部搜索栏的时候，按下方向，在树视图OK；在搜索视图能到最近搜索第一条，再按下，无法继续转移；在最近添加/统计/死链/去重视图，按下，没有反应；
13,图像 images/guide/search-dualzone.pn 截图内容有误，没有切换到搜索视图进行展示
14,在树中定位，如果指定的标签不在书签栏里而设置了只显示书签栏，会导致在树视图定位不到，此刻应该用toast加以提示？或者说当选择了只显示书签栏书签，所有视图都应该限定在这个范围内？请帮我选择一个最符合用户直觉的方案，并给出恰当的提示。
15,搜索的最近搜索区域，按方向右，不会出现右键菜单，违背我们键盘快捷操作的设计
16,重复视图，重复组的菜单项要和重复的items的菜单项加以区分。挑选合适的菜单项目。参考#10,#11
17,选项和高级选项可以合并成一个页面，页面的响应式布局应该更为完善，目前4K分辨率下仅有两列，左下空白也很难看，你应该做出更优雅的布局，且不同选项组非常直观。
18,提供强迫症选项：不显示tab角标
19,选项在在侧栏中打开未选中的情况下，点击扩展图标仍然是打开关闭侧栏，不清楚是不是历史版本数据残留，你需要进行检查修正
20,提供一键复原旧体验的快捷选项，可以把命令面板，快速添加按钮，命令面板按钮，tab等都关闭；同时每一个选项也可以单独开启（几个tab独立开启的选项已经提供），提供充分的自定义能力
