# Module Reference (per-module detail)

> Extracted verbatim from `AGENTS.md` — the detailed half of its "Repository Layout" section. `AGENTS.md` keeps the orientation summary; consult this file whenever a task touches a specific module. **Keep both in sync: when a module row changes, update it HERE (this is the canonical detail layer) and touch `AGENTS.md` only if the grouping/summary changed.**

## Repository Layout (detail)

Runtime code is grouped by kind: first-party JS in `src/`, extension pages in `pages/`, styles in `css/`, vendored third-party code in `vendor/`, images in `assets/` (this is also the layout of the shipped extension — page references use root-absolute paths like `/src/neat.js`). `manifest.json` and `_locales/` must stay at the extension root (Chrome requirement).

| File(s) | Role |
|---|---|
| `manifest.json` | MV3 manifest (`minimum_chrome_version` 114): `src/background.js` service worker (module), `pages/popup.html` action popup, `pages/sidepanel.html` side panel page (opt-in via the `openInSidePanel` setting; `side_panel.default_path` must not contain a query string — Chrome rejects it at install), `pages/options.html` options page, omnibox keyword `*`, permissions `bookmarks`, `tabs`, `favicon`, `storage`, `scripting`, `sidePanel`, `contextMenus`, `tabGroups`, `alarms`, `clipboardWrite`, `proxy` (install-time — Chrome refuses proxy as optional; inert unless a dead-scan proxy server is configured, see `src/dead-proxy.js`), host permissions `<all_urls>`, optional permission `history` (requested at runtime inside a user gesture — see `src/view-stats.js`) |
| `src/background.js` | Service worker (ES module). Omnibox search (debounced 250 ms `chrome.bookmarks.search`, suggestion rendering, sync-status glyphs; ranking/highlight helpers imported from `./search-core.js`) plus side panel management: the behavior derivation lives in `src/panel-behavior.js` (see its row; `minimum_chrome_version` is 114, so `chrome.sidePanel`/`storage.session` need no feature detection), and the `open-side-panel` command (Alt+Shift+B) stamps `sidePanelIsOpen` + `sidePanelHeartbeat` in `storage.session` and opens the panel. The `open-command-palette` command (Ctrl/Cmd+Shift+K, P2) sets a `storage.session` `pendingPaletteOpen` flag and opens the popup via `chrome.action.openPopup` (Chrome 127+), falling back to a `?palette=1` popup window. The `quick-add-bookmark` command (Alt+Shift+S, final polish) bookmarks the active tab straight into `quickAddFolderId` (default `'1'`). Also owns the `vbm-quick-add` page context menu (Phase 3, issue #30): created on install and at every SW startup (remove-then-create for idempotence; the remove→create chain is serialized so overlapping cycles — startup × onInstalled, or a storage flip mid-cycle — collapse into one create instead of raising a duplicate-id lastError), click saves the page into `quickAddFolderId` (default `'1'`); the entry is gated by the `quickAddContextMenu` setting (issue #49, default on — a `chrome.storage.onChanged` listener reads `changes.quickAddContextMenu.newValue` and adds/removes the menu live, no SW restart needed; the setting lives in the sync area since the 2026-08 storage audit, so the SW reads `chrome.storage.sync` with a local fallback for not-yet-migrated profiles — the local→sync migration runs page-side, so the first SW start after an upgrade must still honor the pre-migration local value — and the listener accepts both areas). Also starts the sync engine (P3.6): `createSyncEngine().start()` at top level so every SW cold start re-hooks the listeners. v4 task-2 slice E adds the visit-stats SW collector (`createVisitStatsCollector().start()` from `src/visit-stats-sw.js`): `chrome.tabs.onUpdated` URL navigations matched exactly against a bookmark-URL index bump the same `visitStats` dataset the popup writes — deduped against popup-initiated opens via a 10 s `vbmPopupOpens` marker in `chrome.storage.session`. Also the dead-scan proxy sweep (dead-proxy.js): after the cold-start resume check decides no live run was resumed, when no `vbmProxySession` storage.session marker exists, a best-effort `chrome.proxy.settings.clear` sweeps marker-PAC residue a crashed popup could leave behind (namespace-guarded — `chrome.proxy` is available because `proxy` is an install-time permission, Chrome refuses it as optional; the PAC only proxies marker-tagged probe URLs, so residue is benign). v4 task-4 #16 adds the dead-scan runner (`createDeadScanRunner()` from `src/dead-scan-sw.js`, started top level — pages message it, it publishes the `vbmDeadScan` live blob and resumes mid-run scans after an SW cold start; see its row) and #11 hardened the omnibox `onInputEntered` fallback (a fast Enter ahead of the 250 ms debounce no longer pushes the raw query into `tabs.update` — explicit URLs open directly, anything else falls back to a `bookmarks.search` + `rankBookmarks` top hit). The SW also restores the custom action icon on every cold start (issue #52 — `chrome.action.setIcon` is session-scoped): top level + `onStartup`/`onInstalled` read the stored `customIcon` 19×19 RGBA JSON and rebuild the `ImageData` via `OffscreenCanvas`, so a browser restart no longer falls back to the manifest icon until the popup is opened. The SW glue is unit-tested by `tests/background.test.js` (one import-time chrome double speaking both callback and promise storage styles) |
| `src/panel-behavior.js` | Side-panel action behavior (round-6 extraction; v4 task-3 #19 liveness fix): derives `chrome.sidePanel.setPanelBehavior({ openPanelOnActionClick })` at every SW startup and on storage changes from `openInSidePanel` (option on → always toggle mode) OR a LIVE panel (option off → toggle mode so the next icon click closes a panel opened via Chrome's native entries). Liveness = the `storage.session` `sidePanelIsOpen` marker **plus a fresh `sidePanelHeartbeat`** — the panel page beats every `PANEL_HEARTBEAT_MS` (20 s, `src/popup.js`), `PANEL_STALE_MS` (90 s) is the grace window; a bare marker is crash/session-restore residue and gets removed on read, fixing "the icon keeps toggling the side panel with the option off". 4.0.1 adds the **event-driven death signal**: the panel page holds a `vbm-panel` runtime port; when Chrome destroys it (an action-toggle close — `pagehide` is not guaranteed, and the page's own async reset can be dropped mid-teardown; `sidePanel.onClosed` does NOT exist in the API) the port disconnects and the SW immediately restores popup mode + clears the stale marker when the option is off — so the next icon click opens the popup instead of re-toggling the panel. The gated liveness alarm runs at 20 s (< the 30 s SW idle timeout) so the SW stays alive to receive the disconnect and as a no-port safety net. Unit-tested by `tests/panel-behavior.test.js` |
| `src/search-core.js` | Pure search helpers shared by `src/background.js` and the vitest suites: `rankBookmarks`, `xmlEncode`, `matcher` (no chrome.* references). 4.0.5: `rankBookmarks` delegates its ranking to `src/fuzzy-core.js`, so the omnibox and the popup run ONE fzf-style implementation — only the candidate sets differ (the omnibox ranks the `chrome.bookmarks.search` word-match results, sliced to 6; the popup ranks the whole-tree flat index) |
| `src/escape.js` | Shared HTML escaping (4.0.5 consolidation), pure ESM: `htmlspecialchars` — the single source of truth for what used to be nine verbatim module-private copies (tree-render / dialogs / palette / search / view-recent / view-stats / view-dupes / view-dead / palette-commands all import it now). Escapes `&` (chain head), `<`, `>`, `"`. An earlier revision left `&` unescaped for idempotence, but a full caller audit (all 9 importers + every `highlightTitlePositions` upstream) proved no double-feed path survives — every input is raw data (bookmark titles/URLs, user input, `_m()` messages, settings), and `highlightTitlePositions` escapes char-by-char from the raw title — so `&` was added (completeness over idempotence); `dialogs.js`'s `widont` inserts `&nbsp;` AFTER escaping and is unaffected |
| `src/separators.js` | Separator logic (P1 first module extracted from `src/neat.js`), pure ESM with zero chrome.*/DOM references: `StringList`, `isBlank`, `SeparatorManager` (storage mirror injected via constructor — directly unit-tested by `tests/separators.test.js`) |
| `src/dialogs.js` | Popup dialogs (P1 module): alert/confirm/edit/new-folder/sort dialogs, the 4.0.1 tab-group title/color + existing-group picker dialogs, the 4.0.8 VersionDialog (the palette `/version` command's metadata card: a `<dl>` of extension/announce/browser/OS/channel/language/UA rows, a copy-as-JSON button with clipboard-API → execCommand fallback, a palette-style Esc footer bar), the 4.1.0 CopyMoveDialog (the tab-groups view's copy-vs-move choice for grouping already-grouped tabs) + BookmarkFolderPickDialog (its add-to-bookmark-folder picker) + `#cover`/Escape close handling + global error alert. `initDialogs(ctx)` wires DOM after parse (`ctx.onSort` = folder reindex); pure `widont` exported. The returned object gains `activeEl()` (final polish) — the open dialog's element, backing keyboard.js's modal Tab trap; the ten dialog divs carry `aria-modal="true"`. Unit-tested by `tests/dialogs.test.js` (DOM stub + real-module import); 2026-08: `BookmarkFolderPickDialog.open` accepts `excludeIds` (the folder move/copy cycle guard — banned rows and chips hide while the pin/recent rosters survive unpruned, and a later open without the option resets it) |
| `src/actions.js` | Popup action table (P1 module): all 14 bookmark/folder actions (open in tab/window/incognito, open-all as a color-coded tab group (P3.4 — since 5df7631 the open+group pipeline runs in the service worker, `src/tab-groups-sw.js`, so a closing popup can no longer drop the pending group; plus an "…and set name/color" dialog and an open-into-existing-group variant, sharing `src/tab-group-utils.js`), bulk-open with 10-item confirm threshold, edit via `dialogs`, delete via `ctx.undo.capture` + toast instead of ConfirmDialog, focus handoff, add-bookmark/folder/separator mutations (adding into a collapsed folder expands it and re-renders its children so the new node is immediately visible), copy title+URL via `navigator.clipboard.writeText` (the `clipboardWrite` permission; `execCommand('copy')` fallback — the old execCommand-only path was silently rejected in async callbacks), replace-URL, bookmarklet `__VBM_CURRENT_TAB_URL__` substitution). `initActions(ctx)` receives `store`/`dialogs`/`search`/`separatorManager`/`undo`/HTML builders incl. `generateHTML`; tested by `tests/actions.test.js` (DOM stub + chrome API doubles) — the velvet-staging clipboard family lives here too: `setClipBookmark`/`cancelClipBookmark`/`hasClipBookmark`/`hasCutClipboard` (the session single-item clipboard), `pasteClipBookmarkInto` (folder-end paste), `pasteClipBookmarkAfter` (2026-08: next-sibling paste — a same-parent cut corrects the index for Chrome's remove-then-insert move semantics; an already-next-sibling cut is a clearing no-op), `copyMoveBookmarkTo` and `copyMoveFolderTo` (2026-08: the folder-row picker — passes its own subtree as `excludeIds`, move is a plain `bookmarks.move`, copy recursively clones via the private `copyFolderTree`, separators included as plain URL nodes) |
| `src/context-menu.js` | Popup context menus (P1 module): bookmark/folder/separator right-click menus, item wiring to `actions`/`dialogs`, `switchBookmarkMenu` visibility rules, private `currentContext`. v4 task-3 adds the slim view-row menus and the positional-item rule: stats-view history rows without a bookmark id get the hist-row menu (open×3 via the row href + bookmark-it via the row's own ☆ button, #10), dupes GROUP HEADS get their own menu (apply-dedup with a live keeper/doomed-count label + expand/collapse, #16), and out-of-tree views hide the position-dependent items (`add-*-before/after` etc. via `POSITIONAL_IDS` — invisible tree structure makes them meaningless, #11). `initContextMenu(ctx)` returns `{ clearMenu(e?), closeMenu(), switchBookmarkMenu(disable), bookmarkMenu, folderMenu, separatorMenu, searchHistoryMenu, histRowMenu, dupesGroupMenu, tabRowMenu, tabGroupMenu, tabClosedMenu, tabClosedTabMenu (the four 4.1.0 tab-groups menus), folderTabGroupSubmenu, folderSortSubmenu, bookmarkTabGroupSubmenu, openSubmenuFor, closeSubmenu, toggleSubmenuFor, submenuOpen }` (the last ten may be absent in minimal test setups — consumers null-check; `closeMenu` is the keyboard layer's cancel path) — 4.0.1 focus law: every menu-item dispatch closes the menu FIRST and refocuses the owning row (same-id replacement / list container fallbacks via `refocusOwner`) before the action runs; the open-time positioning clamps the menu to the popup viewport (issue #48: a menu taller than the space below the search bar — the 19-entry folder menu at Windows 150% scaling / page zoom ≥ ~90% — used to make `menu.focus()` scroll the document, and that scroll fired the scroll-dismiss listeners, closing the menu the instant it opened; now the menu gets `max-height` + internal `overflow-y: auto`, chrome-adjusted for its padding/border, so it always fits and focus() never scrolls the page; the real-browser gate is `scripts/harness/verify-menu-overflow.js`). issue #48 follow-up: the tab-group (folder + bookmark menus) and sort (folder menu) blocks can collapse into single "Tab groups ▸" / "Sort ▸" entries (settings `collapseTabGroupMenu` default off, `collapseSortMenu` default on) whose three items live in body-level sibling `<menu class="submenu">` flyouts (`sub-`-prefixed ids, normalized to the parent-menu ids at dispatch; `positionMenu` gained an `entry` flyout mode — anchored to the entry, flipped on horizontal overflow, same #48 clamp, and stacked below the entry when neither side fits; at extreme zoom/resolution the menu's `max-width` is capped to the VIEWPORT (the popup body can outgrow the window) and both axes clamp to `window.innerWidth`; 4.1.0 audit: the clamps are scroll-offset aware (`[scrollX, scrollX+innerWidth]` in document coordinates — the popup body can sit pre-scrolled a couple of px at extreme zoom from startup focus churn, and a viewport-relative clamp parked the menu that far outside the visible band, the menu-extreme rect.l=-2 failures), and menus focus with `preventScroll: true` since a viewport-clamped menu never needs a focus scroll; the folder tab-group entry/submenu reads disabled when the folder has no bookmark children; hover/click/`→`/Enter open, `←`/Esc close, two-level document Esc; the real-browser gates are `scripts/harness/verify-menu-collapse.js` and the DPR×zoom×size sweep `scripts/harness/verify-menu-extreme.js`); initialized first in `src/neat.js` because `search` needs `switchBookmarkMenu` at init. 4.0.4 (a42efea + 8eb9e97) adds the folder menu's content-dependent greying via `applyContentDisabled`: an async `getChildren` read (guarded — a deleted/ghost folder calls back with undefined + lastError) toggles `disabled` on the open*/tab-group entries when the folder has no URL children and on the sort entries when it has no children at all, so an empty folder's menu offers only the add-type entries; the same no-URL rule greys the collapsed tab-group entry + its submenu. 4.0.5: the link-folder branch (folder rows in search results / the palette) goes through `applyContentDisabled` too, and `hideAllMenus` resets the content-disabled state (OPEN/SORT content ids + the collapsed tab-group entry) via clearMenu on every open, so a greyed state never leaks across menus or branches. 4.0.8 adds the view-tab menu: right-clicking a `.view-tab` (or ContextMenu/Shift+F10 on it) opens `#view-tab-context-menu` (Hide/Disable — labels + availability from `ctx.viewMenu.prepare(viewId)`, view-manager's `viewMenuState`; dispatch to `hideViewTab`/`disableView`), and the branch marks the tab `.active` like a row so the document Esc layer dismisses the menu instead of falling through to `window.close()`. The return list gains `viewTabMenu`. 4.1.0 adds the tab-groups view's four menus — tab row, group head, closed group, closed tab — wired to the same dispatch/focus law. 2026-08 clipboard/menu fix batch: `collapseAddFolderMenu` is wired in neat.js (it never was — the class sat permanently on) and now folds the BOOKMARK menu's add-folder pair too (`bookmark-add-collapse` + `bookmark-add-submenu`; the folder flyout entry must stay free of inline display:none — CSS alone decides); `setPositionalItems` writes `''` (not `'block'`) for the visible case so the collapse CSS can still win; the bookmark menu gains `paste-here-bookmark` (tree-only, clipboard-gated → `actions.pasteClipBookmarkAfter`, insert as the target's next sibling) and the folder menu gains `folder-copy-move-to` (root-disabled like folder-edit → `actions.copyMoveFolderTo`). Parked menus pin `top:-999px` (base CSS; `parkMenu()` clears the inline top on every hide) — a never-opened menu's static-position box is zoom-exempt and full-height, and one parked below the fold fed documentElement.scrollHeight (the B2 scrollbar-gate regression `diag-b2-overflow.js` isolates). Unit-tested by `tests/context-menu.test.js` 2026-08-26: the staging-group menu leads with the folder menu's open family verbatim (open-all / open-as-tab-group — titled by the group name / new window / incognito, dispatching `ctx.staging.groupUrls()` through the shared actions; greyed + separator hidden on an empty group), and the tab-row menu gains `tab-row-new-window` → `tabGroupsMenu.moveTabToNewWindow` |
| `src/keyboard.js` | Popup keyboard layer (P1 module): tree type-ahead buffer, `treeKeyDown`/`treeKeyUp` (arrow/Home/End/PageUp/PageDown/F2/Delete nav on `$tree` + search results), menu `contextKeyDown` (4.0.1: all SEVEN menus bound — the separator menu included, its lone entry was the keyboard-unreachable bug — `↑`/`↓` wrap on every platform, the macOS no-wrap exception deleted, `Home`/`End` first/last enabled item, `→`/`Enter`/`Space` execute + refocus the owning row, `←`/`Esc` cancel + refocus, RTL mirrored, confirm on the bare container or a disabled item is a no-op; the toolbar rung `←`/`→` walk now wraps at the edges), and the document-level Escape (close dialogs / quit search) + Ctrl/Cmd+F handlers. issue #48 follow-up: `contextKeyDown` gains submenu branches — the three `<menu class="submenu">` flyouts are bound too; on a `has-submenu` entry `→` opens the flyout and steps into its first walkable item while `←`/`Enter`/`Esc` close it first then cancel the menu; inside a flyout `←` closes only the flyout and refocuses its entry; the document-capture Esc is two-level (first Esc closes the flyout, second closes the menu). v4 task-3 #7 adds the document-level Tab/Shift+Tab zone cycler `tabCycle` (search input → visible header buttons → active view tab → the active view's `.vbm-toolbar` controls → the list's `.focus`/first row, backwards with Shift; yields to the palette/menus, and while a dialog is open traps Tab inside the dialog's own controls via `dialogs.activeEl()` — final polish) — list rows carry roving `tabindex="-1"` so each zone is a single Tab stop. 4.0.5: while the undo toast bar (`#undo-toast`) is visible its button joins the Tab ring as the last stop — a transient bar fixed to the bottom edge; the `hidden` attribute is the visibility signal and the 8 s auto-hide drops the stop again (never an arrow rung — the arrow chain stays stable either way). 4.0.8: the transient banners (`#donation` card, the local `#whats-new` strip, the remote `#announce`) join the Tab ring between the header row and the tab strip while visible — the announce banner's dismiss × is keyboard-reachable this way; hidden banners contribute no stop. The announce banner also joined the Esc banner rung (dismissed through its own ×, so the mark-seen once-semantics stay in announce.js). And tabCycle's row stop (view-manager's `focusDefault` carries the same guard) excludes a `.focus` marker parked inside a `.vbm-dropdown-list` listbox — a hidden option, not a row, so it is never a Tab stop (the 5421968 toolbar-↓ regression lesson, same defense). `initKeyboard(ctx)` receives `tree`/`search`/`actions`/`menus`/`dialogs`/`body`/`os`/`rtl` (all already initialized at its call site) and returns `{ treeKeyDown, treeKeyUp, contextKeyDown, tabCycle }`. 4.0.8: the view-tab menu is bound like the other seven and joins the Tab-trap `menuContainers`; 4.1.0 binds the tab-groups view's four menus (tab row / group head / closed group / closed tab — twelve menus total) the same way, and its row walk skips the non-focusable window section rows; the view-tab strip answers ContextMenu/Shift+F10 with the same dispatch. The undo-toast Tab stop is skipped while the toast button is `hidden` (a buttonless `toastAction` hint contributes no stop). No neatools inside. Unit-tested by `tests/keyboard.test.js` |
| `src/dnd.js` | Popup drag & drop (P1 module): tree mousedown drag start (rejects root folders / `data-virtual` recent entries), document mousemove drop-target tracking with `#bookmark-clone` ghost + `#drop-overlay` insertion line, edge auto-scroll, mouseup drop → `chrome.bookmarks.move` with cross-storage guard (`canMoveBetweenStorage` blocks synced↔local moves and explains via an on-demand `#notice-toast` — the old `alert()` destroyed the popup window). `initDnd(ctx)` receives `tree`/`store`/`rtl`/`resetSeparator`; returns `{ isDragging(), consumeNoOpen() }` — the click handler and zoom read drag state through these. No neatools inside.  2026-08-28: #bookmark-clone and #drop-overlay are `position: fixed` (css/neat.css) — as absolutely-positioned body children the clone swinging out at the popup edge EXTENDED the document's scrollable overflow (repro diag/diag-edge-drag.js: body scrollWidth 320→406 mid-drag), which in the real popup (viewport == body width) made the whole view horizontally scrollable: a wheel/trackpad pan mid-drag shifted the view left with a blank strip on the right. Fixed boxes never contribute to scrollable overflow; the JS coordinate math is unchanged (body sits at the viewport origin and never scrolls, so viewport coords == the old body-absolute coords, including the /zoomLevel conversions). Unit-tested by `tests/dnd.test.js` |
| `src/icons.js` | Inline SVG icon constants (P4 — retired the folder.png / document-code.png bitmaps): `FOLDER_ICON`, `DOCUMENT_CODE_ICON`, plus `CHEVRON_ICON` for folder twisties, `VIEW_ICONS` (v4 task-2: one 16px icon per view for the tab strip) and `DEFAULT_BOOKMARK_ICON` (4.0.2 — the no-favicon globe swapped in by `src/favicon-fallback.js`). Line-style per docs/现代化演进总方案.md (16px grid, 1.5px stroke, `stroke="currentColor"`), colored by CSS `.vbm-icon-*` rules; consumed as HTML strings by tree-render/search/palette row templates |
| `src/favicon-fallback.js` | Default-favicon fallback (4.0.2): Chrome's `_favicon` placeholder for favicon-less pages is a flat-gray bitmap that nearly vanishes on dark/ink (the 4.0.1 CSS brightness lift was partial). At init the module fetches the icon for a `.invalid` URL (always the placeholder) and fingerprints its pixels (`hashPixels`, FNV-1a over RGBA); a capture-phase `load` delegation on the document swaps any favicon `<img>` with the identical fingerprint for the currentColor `DEFAULT_BOOKMARK_ICON`. Verdicts cached per src; inert when `_favicon`/canvas unavailable. 4.0.5 adds the favicon contrast-invert service: from the SAME `getImageData` buffer the placeholder check already samples, `contrastStats` derives the fraction of opaque pixels on the dark extreme / the light extreme / the opaque coverage, and `needsContrast` flips a monochrome icon that would vanish on the theme's background — the verdict reads extreme-tone SHARES, not mean luminance+saturation (a mean is fooled by plate-style icons: x.com's white-X-on-black-plate averages "very dark", and flipping it would turn the elegant self-inverting design into a glaring white plate). On a dark background the flip needs dark > 0.55 with light < 0.05; on a light background light > 0.60 with dark < 0.15 AND colored < 0.30 — the colored guard (the fraction of opaque pixels with sat > 38) keeps a colorful logo like the Chrome WebStore devconsole icon (colored ≈ 0.42) from being inverted into a black-card-with-complementary-colors wreck on a light theme, while yabook's pure-white glyph (colored 0) still flips; mid-tone and two-tone icons fall between the guards and never flip. 4.1.0: the dark branch gained a STRICTER colored guard (< 0.10) — the flip's invert+hue-rotate halves a vivid mark's chroma (measured netflix red 190→90, a washed pastel users reported as a "weird filter"), while the saturated original reads fine on dark by chroma contrast alone; the guard stops netflix's dark-red N and the old youtube .ico's red plate, keeps pure-black marks (github/thepaper, colored 0) flipping, and is deliberately stricter than the light branch's 0.30 (wrongly flipping a colorful mark is a glaring defect; wrongly not flipping a dark mark is merely dim). Retuned against a 60-icon matrix (the original 14 + tmp/favicon-lab/icons2's 46 common sites — netflix/youtube/figma/tiktok/x/bilibili/zhihu/taobao/jd/weibo/… , favicon.im-fetched) with a visual comparison sheet (tmp/favicon-lab/sheet-2.py). Also 4.1.0: the default-icon SVG template is parsed once and cloned per swap (a 1371-row re-render used to re-parse the same markup per placeholder row, ~40 ms). The flip itself is CSS `invert(1) hue-rotate(180deg)` (`.favicon-contrast-invert` in neat.css) — a lightness-only flip that preserves hue and saturation, so the netflix red keeps its hue. An `auto` theme OS-level switch re-decides via a `matchMedia('(prefers-color-scheme: dark)')` change listener (cheap, no re-sampling), and the options page's `faviconContrast` switch (default on) reaches an already-open panel through a direct `chrome.storage.onChanged` listener in neat.js — the store mirror has no onChanged forwarding. 4.0.6 favicon enrichment hook: `ctx.onPlaceholder(img) → boolean` lets the enricher (`src/favicon-enrich.js`) handle a placeholder first (cached-icon hot-swap returns true; else enqueue + false → default SVG as before) on both placeholder branches; the API gains `sampleIcon` (the internal fingerprint, for the enricher's contrast registration of injected data-URL icons); `reapplyContrast` also covers `img.favicon-enriched`. Unit-tested by `tests/favicon-fallback.test.js` |
| `src/favicon-enrich.js` | Missing-favicon enrichment (4.0.6, docs/plan-4.0.8/favicon-补全设计.md): fetches real icons for hosts Chrome's `_favicon` has no cache for. Discovery chain L1-L4 — L1 `GET https://<host>/favicon.ico` → L2 parse the bookmarked page's `<link rel=icon>` (attribute-level regex; at most 5 scored candidates; `data:` hrefs pass straight through with base64 or percent-encoded decoding, and a malformed one skips just that candidate) → L3 proxy relay (the dead-scan's marker-PAC session, when live, retries L1/L2 via `addProxyMarker`; each breaker-tripped aggregator gets one proxied retry) → L4 the built-in aggregator list (favicon.run → icon.horse → DuckDuckGo) as the FINAL means (default-on sub-switch, per-provider breaker 6h + automatic failover — each provider's quirks are normalized behind a consistent `url(host)` + `interpret(res, networkOk)` → 'icon'/'no-icon'/'unreachable' interface; a clean no-icon fails over to the next provider, an unreachable one trips that provider for 6h; icon.horse answers unknown hosts with a 200 + `image/png` letter-avatar tile (deterministic per first letter — live-verified 2026-08-19, refuting the original clean-404 assumption), so an icon-horse 'icon' verdict that passes validation is re-checked against a per-letter reference probe (`placeholderProbeUrl` → `<L>-vbmref.invalid`, fingerprinted with favicon-fallback's `sampleIcon` FNV-1a — identical dimensions + hash = avatar → verdict flips to no-icon and failover continues; the avatar never reaches the 30d success cache; one probe fetch per letter per session, any probe trouble fails OPEN and never trips the breaker). Each layer's output passes a four-step validation (res.ok + ≤200KB + Content-Type/magic sniff + Image decode). Cache is per-host keys `vbmFavicon:<host>` = data URL + one index `vbmFaviconIdx` {v, down, hosts} (v3; per-provider breaker table, legacy v1 indexes migrate on hydrate keeping hosts, dropping only the stale breaker window), with a dynamic byte budget = (chrome.storage.local quota − bytes used by other features) × 0.8 (floored, capped at the real free space), halving eviction (cut the oldest half) when persisted bytes exceed it, >96KB icons session-only, quota-error emergency eviction (reuses the same halving), index-rebuild/self-heal on corruption/drift. Renders block only on an in-memory Map read; ≤6 concurrent fetches, per-host dedup, failed markers back off 24h → 3d → 7d then give up permanently (the `f` failure count climbs per retry, `failedState` derives gaveUp/retryAt; 4.0.8 收敛策略 — success entries NEVER expire: a cached icon is served until budget eviction or a cache clear removes it, so no periodic re-fetch storm and no endless retry of impossible hosts; retries only trigger from placeholder renders, never a background poll). `setEnabled(false)` aborts every in-flight item, and an aborted run never stamps the failed marker (4.0.8 收尾: the discover layers swallow the abort into a null, which used to be misread as "host has no icon"). The options-page `storage.onChanged` clear path now also drops individual hosts when only their `vbmFavicon:*` data keys are removed, not just on index removal (4.0.8 低优先级收尾). Wired by `src/neat.js` via a lazy `ctx.onPlaceholder`; live switches `faviconEnrich` (default on) / `faviconEnrichAgg` (default on). The hot swap fades the new `<img>` in and adds a 0.38 s `.favicon-pop` animation only when the row is in viewport (`inViewport`) — both silenced under `prefers-reduced-motion`. 4.0.8: every success entry records WHERE the icon came from — index host entries carry `src: 'direct' | 'proxy' | <provider-id>` (legacy entries simply lack it), threaded discover → writeEntry → persistIdxNow/hydrate/emergencyEvict; the favicon gallery page reads it (`src/favicon-gallery.js`). Unit-tested by `tests/favicon-enrich.test.js` |
| `src/tree-render.js` | Tree HTML + data helpers (P1 module): `buildRowTooltip` (issues #62/#64: the ONE full-info tooltip builder every view bakes at render — title / URL / labeled Path (canonical root-first) / labeled Added (toLocaleString) / append line, all segments escaped internally, future metadata appends labeled lines), `formatPath` (canonical root-first ' / ' join — tooltips ALWAYS use it) and `formatPathLabel` (issue #64: the FULL chain NEAREST-parent-first `A < B < C`, NO depth cap — overflow falls to the same CSS ellipsis the canonical order uses, which then eats the distant ancestors — the meta-line form the reverseItemPath option selects; buildPathMap/buildTreeSnapshot carry BOTH as the parallel `paths`/`pathLabels` maps; generateHTML/nodeHtml/generateTreeBlocks thread the canonical map so TREE rows' tooltips resolve their path O(1) from the snapshot, and `meta.tooltipOnlyPath` (tree rows + tabgroups tab rows) feeds the tooltip while suppressing every label slot — the tree IS the hierarchy, its rows never grow path labels / a second line), `getFaviconUrl`, `highlightTitlePositions` (`<mark>` wrapping), row builders `generateBookmarkHTML`/`generateFolderHTML`/`generateSeparatorHTML` (sync indicators, localized `(Local)`/`(Synced)` dual-storage root suffixes via `syncSuffixLocal`/`syncSuffixSynced`, `unsynced-subtree` marking on `syncing === false` rows for the highlightUnsynced dimming, separator color via pure `colorHex`), recursive `generateHTML` (open-state from ctx getters, lazy `getChildren` expand, `(Empty)` rows), and pure tree helpers `generateNodeTrees`/`buildTreeSnapshot`/`getParentPath`/`findFolderByType`/`getEffectiveSubTree`/`isRootFolder` (dual-storage aware). 4.1.0 P1-1: `buildTreeSnapshot(tree, subTree)` is the single-walk snapshot — one full-tree traversal produces `{ html, nodeTrees, bookmarkIds, paths, ids }` (paths/ids cover the FULL tree for list-row labels + visitStats.prune; html/nodeTrees/bookmarkIds follow the display subtree tree-view selected), so generateTree no longer repeats generateNodeTrees + addBookmarkParents + buildPathMap. `initTreeRender(ctx)` receives `store`/`separatorManager`/`getOpens`/`getRememberState` (getters, read at call time); neat.js feeds the builders into the search/actions ctx. No neatools inside. 4.0.5: the untitled-bookmark display-name fallback (protocol-stripped URL) is escaped through `htmlspecialchars` like its tooltip twin — a pre-v4.0 gap. Unit-tested by `tests/tree-render.test.js` 2026-08-26: the tree-row 暂存 plane also stands down when the staging VIEW is disabled (`stagingRelayOn` requires showRecentBookmarks on — the disabled-view contract of the staging master switch) 2026-08-28 perf 任务①: the row-tail icons ride the document-level ICON_SPRITE_SHEET (icons.js — 18 symbols, generated by scripts/icons/gen-sprite.mjs from the inline exports, byte-identical, contract-tested); neat.js injects the sheet at startup and ensureIconSheet stays as the test-environment fallback; the sprite recipe extends to the list views' row/head buttons (tabgroups rowIcons ×5, group/closed/window heads, dupes head+rows, staging rows/heads/bucket, stats/dead/search rows via staging-relay's optional icons arg — flipStageBtn included) |
| `src/tree-view.js` | Tree view layer (P1 module): owns the tree DOM — `generateTree` (+ startup `getTree` bootstrap, focusID/scrollTop restore — issue #58: the focusID restore (refocus + blueFade reveal highlight) is gated by the remember-state flag like the scroll/folder restore (4.1.1 分层记忆: scroll → rememberScroll, opens → rememberOpens (tree-render), focusID/focusSpot → rememberHighlight, query → rememberSearchQuery), so the remember-prev-state option turns the whole "where I was" restore off; `revealFolder`/`revealInTree` force remember-state on so explicit reveals keep working; legacy local-separator migration; 4.1.1 full-info round: the lazy folder-expand renders rows OUTSIDE generateTree and passes the LAST snapshot's canonical path map (`lastPathsMap`) so its rows carry the Path tooltip line too; issue #64: the focusID restore also stands down under ctx.getFocusSearchOnOpen, dropping the stale focusID eagerly), tree events (scroll persist, focus tracking, click expand/collapse with lazy children + `closeUnusedFolders`, middle-click focus), and `bookmarkHandler` (click/auxclick open semantics shared by every list view — tree/search results/recent/stats/dead/dupes rows all bind it; final polish: the search results pane also binds `auxclick`, closing a legacy middle-click gap; v4 task-2 slice D fires the optional `ctx.onOpenBookmark(id, url)` hook from its bookmark branch, the single page-side visit-stats collection point). The v4 "recently added" in-tree section is gone (slice B moved it to `src/view-recent.js`). `initTreeView(ctx)` receives `store`/`tree`/`separatorManager`/`SeparatorManager`/`treeRender`/`search`/`actions`/`dnd`/`refreshSyncIndicators`/open-state getters+setters/click-mode flags/`views`/`onTreeGenerated` (rebuilds the view path map + dead-mark overlays + visit-stats prune on every tree build)/`toastAction` (v4 task-3 #14: the undo bar's generic action toast); returns `{ generateTree, revealFolder, revealInTree, bookmarkHandler }` (issues #62/#64 full-info round: the H2 adaptive-tooltip machinery — `adaptBookmarkTooltip`/`adaptBookmarkTooltips` + the delegated mouseover/focusin pass, and resize.js's drag-end re-measure — is RETIRED; every tree row bakes its complete tooltip at render time via tree-render's buildRowTooltip: 标题/URL/路径/添加时间). `opens`/`rememberState` stay in neat.js, shared via ctx. v4 task-3 #14: the `onlyShowBMBar` subtree filter gained a session-only `showAllOverride` — `revealInTree` on a target with no `nodeTrees` entry (outside the bar) toasts a hint with a "show all and reveal" action that regenerates over the full tree and completes the reveal, never rewriting the setting. No neatools inside. Unit-tested by `tests/tree-view.test.js` 2026-08-27 real-data perf round: tree rows join the 4.1.0 content-visibility list (cv:auto + contain-intrinsic-size auto 1.67em — the row-height formula itself), killing the offscreen style/layout/paint that dominated the maintainer's 5161-URL cold open (trace: UpdateLayoutTree 2715→831ms, Layout 1561→177ms, Paint 1307→108ms); the old 'geometry-sensitive' exclusion was re-audited item by item (tooltips are H2 per-row on demand, dead ×/sync dots are in-row absolutes, reveal scrollTo+focus auto-renders the target) — verify-keyboard/scrollbars/menu all green, 5005-row scrolled visual probe passes 2026-08-28 perf 任务④: CHUNKED first paint for big shallow renders — buildTreeSnapshot now also returns top-level blocks (nodeHtml factored out of generateHTML; html === wrapper + blocks.join, one code path); generateTree streams the first ~3 viewports synchronously then per-rAF (scroll/focusin/dragstart flush synchronously; a remembered deep scrollTop OR a focusID memory stays on the one-shot swap so every restore path is instant; jsdom doubles fall back to the sync swap) |
| `src/sync-ui.js` | Sync indicator wiring (P1 module): subscribes to SyncManager's `syncStatusChanged` window event and rebuilds the `.sync-indicator` badges on tree/search-result rows (`updateBookmarkSyncStatus`, `refreshSyncIndicators`). `initSyncUi({ store })` runs the wiring on init (DOMContentLoaded-aware) and keeps the legacy `window.neat.refreshSyncIndicators` surface. No neatools inside. Unit-tested by `tests/sync-ui.test.js` |
| `src/view-manager.js` | View system core (v4 task-2 slice A, docs/plan-4.0.0/v4task-2.md §3): the seven-view registry (`tree`/`search` structural + `tabgroups`/`recent`/`stats`/`dead`/`dupes` registered by their modules — 4.1.0 added `tabgroups`; registration order = tab order), the `#view-tabs` strip (16px inline-SVG icons from `VIEW_ICONS`, localized labels, count badges gated by `showTabBadges` since v4 task-3 #18, `showViewTabs` master toggle, `←`/`→` cyclic tab move with dir-aware RTL, strip `Home`/`End` view-scoped — first/last row of the CURRENT view via `focusEdgeRow`, never a view switch, no rows → focus stays on the tab), the single-active-view state machine (`activate(id, { preset })` = deactivate → display switch → activate → persist `activeView`/`viewState`; the optional preset is forwarded to the view's `activate` — v4 task-4 #6 view-preset custom commands drive the dupes strategy/scope and the dead scan-start through it); the sliding `.tab-indicator` (`placeIndicator`) is clamped to the strip — `offsetLeft`/`offsetWidth` round independently, so on a fractional-DPR layout the last tab's indicator could overshoot the strip by 1px, and that absolutely-positioned 1px overflow propagated through the unclipped `#view-tabs`→`#container`→`body` chain into the document scroll width, which Chrome uses to size the popup window (4.0.8 report: the popup widened 1px only while the dupes tab — the strip's last — was active; the clamp floors at 0 and skips itself when `clientWidth` is absent, keeping unit stubs unchanged); startup restores the stored view when `rememberView` is on — default since v4 task-3 #6, panel always restores, an unregistered stored id waits in `pendingRestore` until its module registers), per-view focus/scroll memory (v4 task-3 #7: `viewState` migrated from a bare scrollTop number to `{ scroll, focus }` with old-number compat — leaving a view records scrollTop + the `.focus` row id, a `focusin` marker keeps mouse-clicked rows trackable, and entering re-marks the row via a 100 ms×20 watchdog that survives async view re-renders), the shared bookmark-id → parent-path map (`buildPathMap` on every tree rebuild backs every list view's §3.6 path labels via `pathOf`/`showItemPath`), the aria-live `viewSwitchAnnounce`, and the Esc layering (palette → dialog → search → back to tree). View-jump keys (v4 task-4 #10): `Alt+1…9` over the visible views is the portable binding (Edge reserves `Ctrl+1…8` for browser-tab switching); the legacy `Ctrl/Cmd+N` twin still fires where the browser lets it through, never inside inputs, and Ctrl+Alt (AltGr) combinations are excluded. 4.0.5: the remembered-row restore resolves its focus target through `src/list-focus.js`'s `rowFocusTarget` contract (the row's anchor/span, or the tabindex row container) instead of a `firstElementChild` heuristic — a button-led row (the dupes member rows lead with the keeper radio) made the old restore focus a tabindex-less `<li>`, a silent no-op — and `viewState` (per-view scroll + remembered row) is now gated by the same remember-state option as `focusSpot` and the tree restore (the path-map API also exposes `pathsReady()` — issue #64's boot-order heal contract — and `pathLabelOf(id)` / `dateAddedOf(id)`, the meta-line path form (canonical by default, nearest-first label map under the `reverseItemPath` option, default off; tooltips stay canonical either way) and the id→dateAdded map (the Added tooltip line's source for staging/visit-stats rows whose own models lack it); issue #63: the built-in TREE registration carries `persistScroll` too — a view round-trip no longer wipes its scrollTop via the container display:none; issue #64: `restoreFocusSpot` stands down under ctx.getFocusSearchOnOpen): off means never written on view switches and never restored on activate, and a stale stored key from a remember-on session is dropped once at startup. `initViewManager(ctx)` returns `{ register, attach, activate, activeId, activeDef, isActive, views, lists, listOf, onEscapeActive, escapeToTree, focusTop, focusActive, focusEdgeRow, buildPathMap, pathOf, updateBadges, showItemPath, isAvailable, availableViews, viewMenuState, hideViewTab, disableView }`. 4.0.8: view hide/disable — the tab strip's right-click menu (Hide/Disable, `ctx.viewMenu`) and the options page's per-view Enabled/Disabled state are driven from here. The legacy `show*View` keys mean HIDDEN (tab + `Alt+N` gone, palette command stays, entering a hidden view toasts the Esc/palette way back via `ctx.toastAction` — an intentionally buttonless toast); the new `disable*View` keys (`disableRecentView`/`disableStatsView`/`disableDeadView`/`disableDupesView`/4.1.0's `disableTabGroupsView`, `store.KNOWN_KEYS` members) mean fully forbidden (`activate` refuses, palette rows drop). Tree/search can't be hidden — when only they remain, their Hide item collapses the whole strip (`showViewTabs` off + `body.no-view-tabs`). Hiding the ACTIVE view keeps it active; disabling it switches to the first visible view; `Alt+1…9` indexes the visible set so numbering compacts. A `chrome.storage.onChanged` listener + `store.adopt` live-syncs an open popup/panel with options-page writes. 4.0.8: any real view switch dismisses the transient toast (undo bar / hidden-view hint) via `ctx.dismissToast` — and because the manager's first activation runs inside initViewManager, neat.js initializes `initUndo` BEFORE `initViewManager` and passes `toastAction`/`dismissToast` as plain values (2026-08 audit: the key originally sat on initTreeView's ctx where nothing reads it, and the first lazy-getter fix threw a TDZ ReferenceError on the first view switch — caught by the Docker smoke gate, invisible to the unit suites). Unit-tested by `tests/view-manager.test.js` |
| `src/list-focus.js` | Row-focus park/restore shared by the four list views (recent/stats/dead/dupes) and the search view's history area (4.0.5 consolidation of their verbatim copies — the 4.0.1 focus law's mechanism): `parkRowFocus`/`unparkRowFocus` (a render's innerHTML swap replaces every row, so the focused row is parked before the swap and restored after — by row id when it carries one, else by its index among the list's `<li>`s, clamped on restore so a vanished row lands on the row that took its place; an emptied list falls back to the container itself, or to the caller's `emptyFocus` — the parameter that absorbed search.js's history-area "no rows left → focus back to the search box" copy), `rowFocusTarget` (the row focus-target contract — `querySelector('a, span')`, or the row container itself when it carries tabindex; never a `firstElementChild` heuristic, so a button-led row still resolves to its anchor; shared by view-manager.js's remembered-row restores), and the toolbar focus trio `TOOLBAR_CONTROLS_SEL` + `parkToolbarFocus`/`restoreToolbarFocus` (the stats/dead/dupes toolbars re-render together with the rows — a focused control is parked as its first class token + index among same-class controls, the same key view-manager's focusSpot uses, so a re-render that adds or removes a different kind of button does not drift the restore the way a bare position index would; the selector includes `.risk-banner` controls so dead/dupes' banner rides along). Unit-tested by `tests/list-focus.test.js` |
| `src/list-chunks.js` | Chunked list painting (4.1.0 perf, driven by the diag-41x-perf.js findings): `paintListChunked(list, { head, pieces, first, chunk, onHead, onChunk, onSettled })` — the heavy list views (tab-groups, dupes) used to pay 1-2 MB of row markup in ONE innerHTML assignment (~470 ms parse for 2508 dupes rows, ~1.7 s parse+layout for a 1371-row tab-groups refresh). The helper paints the head (toolbar + the closed `<ul>`) SYNCHRONOUSLY, discovers that `<ul>` in the fresh DOM, appends the first rows INTO it, then streams the remaining pieces inside it in `requestAnimationFrame` batches — pieces NEVER concatenate after the closed `</ul>` (in a real parser the `<li>`s would strand as siblings of the list, invisible to the keyboard row walks and the `#x-list ul li` CSS rules — the D1 keyboard-gate regression the Docker tier caught, invisible to the string-concat unit doubles) — first content lands in the first frame, the total work is unchanged. Degrades to a single synchronous innerHTML when rAF is absent (unit-test doubles, node), the list lacks querySelector/insertAdjacentHTML, or the list fits in the first chunk; a head without a `<ul>` repaints everything synchronously (no callback has fired yet). The scheduler is resolved per CALL (not import time). The returned handle's `cancel()` drops pending batches — every render cancels its predecessor (the views hold a `paintHandle`). Callback contract: onHead right after the head paint (toolbar focus restore — the toolbar lives in the head), onChunk(list, from, end) after each appended batch WITH the slice bounds (the views gate piece-indexed focus retries so no per-batch whole-list scans run), onSettled once the last piece is in (clamped-index row restore is only safe with the full list present). 4.1.0 perf round 2 (report docs/review-4.1.0/perf-round2-audit.md): `adaptive:true` re-sizes each subsequent batch from the MEASURED insert cost toward `budgetMs` (grow ×1.6 below 40% budget, shrink proportionally above, clamped to [minChunk, maxChunk] — the fixed 42-frame staircase at 2508 rows becomes machine-paced), and `pipes: [{ ul, pieces, first, chunk }]` streams several `<ul>`s from ONE head paint (the dead view's result list + marked-residue list; each pipe's `first` slice lands synchronously with the head, the same contract as the single list). Perf round 3 (2026-08-24): pipes mode is now adaptive too — one shared scale over every pipe's base chunk, fed by the WHOLE round's insert cost (all pipes share one frame budget); previously `adaptive` was silently ignored in pipes mode. The fold-during-stream cancel contract (f9d9e1b) is pinned by a dedicated test. Unit-tested by `tests/list-chunks.test.js` |
| `src/virtual-list.js` | Virtualized list painting, 4.1.0 实验室 (`virtualScrollLab`, options Labs group, DEFAULT OFF — promoted only after real-world soak; the P2 virtual-scrolling item in pre-release form). Same head/pieces contract as list-chunks.js; the views pick the painter per render from the flag (live: a chrome.storage.onChanged listener adopts the value and re-renders). Keeps ONLY the viewport window (+8-row overscan) in the DOM: piece heights are estimated (li-tag count × the 28px row height, group blocks = head + members), off-screen geometry is two paddings on the rows <ul>, scroll re-windows via a passive scroll listener (rAF-throttled), and a focusin edge extension pages the window when the first/last rendered row takes focus (a held ↓ keeps advancing). The pre-paint scrollTop is captured BEFORE the head swap (a real DOM clamps scrollTop when content collapses — reading after silently re-windowed to the top on every re-render); `revealIndex` seeds the initial window around a piece (the tab-groups first-activation current-tab scroll). 4.1.0 perf round 2: piece heights are MEASURED after every applied window (per-piece li `offsetHeight`, prefix sums rebuilt, paddings written from the UPDATED geometry) — visited regions get exact heights, so wide/panel two-line rows stop drifting the scrollbar (the LAB's chief limitation); unmeasurable pieces (unit doubles, never-laid-out cv:auto rows) keep their estimate. Remaining lab limitations: End/Home land on the last/first RENDERED row; clamped-index focus restores are approximate (id-based restores work — the handle's `partial` flag lets the views skip the index path). 4.1.0 perf round J: content-visibility:auto is mutually exclusive with the painter — a fresh window's rows skipped rendering AND hit-testing until the next scroll (blank viewport at 6000 bookmarks, repro diag-vl-6000.js), so the views toggle a .virtual-paint class and css/neat.css forces content-visibility:visible on those rows (cv buys nothing at ~40 windowed rows anyway; measured heights also become exact). Fold surgery round: the handle exposes `fold(from, to, shouldHide)` — hide/show a CONTIGUOUS piece range with no repaint (hidden pieces keep their last-measured height, contribute zero to tops; the range's rows are detached into a per-piece DocumentFragment and reinserted as the ORIGINAL nodes on show; paddings — the virtual scrollbar — follow the rebuilt tops exactly, a block entirely above the viewport compensates scrollTop so the view stays anchored, and pieces that leave the window are trimmed so painted rows + paddings always equal the window span); `hiddenRanges` opts let a render start pieces hidden so render-time folds expand surgically. The views feed it fold-shaped pieces (head and member body as SEPARATE pieces).  2026-08-28 audit fix round: external geometry changes invalidate the cached prefix sums (extension zoom rescales #container's internal layout space, a popup height drag changes the viewport, a width flip across the 480px container query turns rows two-line) and NONE of them fire scroll — a stale model pinned the scroll range so the list TAIL became unreachable at 120% zoom (repro diag/diag-vl-zoom-drag.js: bottom paddingBottom wedged at 188px, un-zoom and even a re-render with a real-px savedScroll didn't heal). Two ResizeObservers now feed a relayout(): the list's box (viewport change → re-window only) and the rows ul (its rendered height drifting from totalH() means ROW heights changed → heights reset to estimates, tops rebuilt, the window around the live scrollTop re-applied — the fresh-paint state, which re-measures the window); apply() always leaves ul height == totalH() (the paddings ARE the model) so the observers' own writes don't re-trigger; a baseline delivery primes the guard, cancel() disconnects both, and `handle.relayout()` exposes the recovery for tests/callers. Verified: diag phases B/C/D/E/H all blank 0/5 with the bottom padding 0 (tail reachable, un-zoom heals). Unit-tested by `tests/virtual-list.test.js` |
| `src/view-recent.js` | Staging view (velvet staging, keeps the `recent` view id/`#view-recent` container/`showRecentBookmarks`/`disableRecentView` keys — the tab title reads "暂存区"/"Staging"; palette `/recent` gained the alias `staging`): the upgraded recent tab as a decision workbench. ONE scroll container `#staging-list` (the registered listEl, first `persistScroll` user) holds sibling `<ul>`s — `#staging-items` (dual-state rows: `id`-anchored = bookmarked with a REAL-state star, `id = null` = url/title snapshot with a hollow star; render order ① the unbookmarked inbox bucket head with "new N" since `lastSeenTs` + favorite-all ② groups by createdAt ③ bookmarked loose rows) then the foldable `#recent-head` section divider (count + stage-all) and `#recent-list` (the classic getRecent rows, now with a hover ↑ stage toggle) — crossRowUl walks the regions. The `staging` local key persists the model (src/staging.js), store debounced + explicit `views.updateBadges()`; badge = item count. Tree-event sync: onCreated promotes matching id-less rows, onRemoved verifies per-URL through `chrome.bookmarks.search` (relink or fall back to id=null — never silently drops), onChanged updates snapshots, every tree rebuild feeds `onTreeSnapshot` for a full urlIndex relink (buildTreeSnapshot grew a `urlIndex` map), and `chrome.storage.onChanged` replays the whole object from the other document. Selection mode (dead/dupes machinery, two rungs): open/open-in-group/favorite/un-favorite (REAL create/remove — items stay)/group-assign dialog/move-copy via the extended picker (>10 move confirm)/delete (confirm + toastAction undo restoring bookmarks AND items)/remove (tree untouched)/clear (confirm); group + bucket heads are tri-state select-all units; entering expands all folds via snapshot; Esc exits. Group-level homing: head hover button + the staging group menu's save/copy-to-folder. The returned `{ refresh, api, onTreeSnapshot }` feeds neat.js's lazy `staging` ctx (context-menu send entries, stats history-row button, tabgroups interop). UX rounds: the send glyph is the paper plane (STAGE_ICON line / STAGE_ICON_DONE solid pair), the per-bucket recently-added heads carry a hover send (bucket membership fixed at render time), the group menu gains select-all-in-group (one hop into selection mode preselected), and the row-menu "Copy/move to…" on an unbookmarked row routes through api.moveCopyItem (the §3.3 favorite-into-folder semantics — its li id is a data ordinal, not a bookmark id).  Workbench round (2026-08-23, the 9-point review): (perf) chrome.storage.onChanged fires in the SAME document that wrote — every persistStaging echoed back ~200ms later as a phantom replay (double render + a stagingState swap that could strand batch closures on the stale object); the listener now skips echoes byte-identical to any of the last 16 strings we flushed (`ownWrites`), tree-event promotions/snapshot edits/relinks mutate state synchronously but coalesce their persist+render into one 120ms trailing tick (`commitStagingSoon` — a folder-send batch previously re-rendered the full list once per created bookmark), and probePermission only repaints when the history-permission verdict CHANGES (view entry painted twice before). (groups) the idle toolbar never disappears: summary left, the action cluster right-pinned (dead/dupes law) with 新建分组 (NewFolderDialog → manual group, empty manual groups render their head and survive pruning) + select-mode (hidden at 0 items); the group head carries the tabgroups-style hover quick tail [rename EDIT][place FOLDER_STAR] on the rows' right axis and F2 renames the focused head; the group menu gains 删除分组 (deleteGroup = group + members leave, confirm + toast-undo via restoreGroup). (DnD) staging rows are HTML5 drag sources (anchors opt out with draggable="false" so the li is the source; the tree's mousedown drag skips data-virtual rows anyway) — drop on a group head assigns (collapsed targets auto-expand), on the bucket head ungroups, on a row adopts that row's group, and dragging a group HEAD onto another reorders (`reorderGroups`, manual arrangement is staging-only bookkeeping). (CSS) the duplicated stale staging/folder-pick/toolbar blocks were removed from neat.css (~480 lines — the second copy overrode the new recipe: the collapsed chevron showed BOTH the ▸ glyph and the border-drawn icon, heads lost the fixed row height/8px rhythm); heads share the tabgroups/dupes recipe (8px lead via 4px gap + 4px glyph margins, 4px end padding, title flex:1, :focus protocol, shared 14px count pill), the HIERARCHY is tabgroups-style — member rows (bucket members included) indent 16px so their favicon column (32px) sits exactly under the group head's title / bucket head's star, loose rows keep the 16px baseline, narrow rows are `min-height: --vbm-row-h` (28px, the heads' height), and selection mode keeps the checkbox on the shared 8px axis while stepping the member anchor margin 28px (favicon 56px = the selecting heads' title/star column) — and every trailing button is a plain .row-btn vertically centered on ONE 28px-stride right axis shared with the toolbar's rightmost icon. The group quick tail is four always-visible buttons ([rename][place][dissolve][remove 移出暂存] — the danger delete-group was pulled off the head into the menu/selection mode, the rightmost slot is now the tree-safe group removal with confirm + undo; ≤400px folds rename/dissolve away) and the scissors `.staging-cut` divider separates the recent region (whose time heads share the 11px/8px/28px style). The selection action rung is iconified (nine 22px glyph buttons, title/aria labels) and `openBookmarksInGroup` was fixed for the urls-only call path (`pickGroupColor(undefined)` crash); a THIRD rung is the customizable move-to shortcut bar (`stagingShortcuts` local key, tabgroup-color dots + alias chips — normal mode is zero per-chip chrome: click = MOVE, manage lives in the right-edge [+]/[pencil] cluster, edit mode gives chips a dashed accent border (click = edit) and a floating red × over the color dot for delete; the editor is `dialogs.StagingShortcutDialog` — target folder via the shared picker's legacy single-select, alias input, the nine tab-group color swatches; move only, copy stays on the icon rung/menus). The action rung is width-aware: `.staging-btn-label` spans appear progressively (danger pair at ≥520px container, organize/favorite at ≥680px, all at ≥820px), and the shortcut bar's 「收藏到：」 label shows only ≥520px. The folder picker's filter input now caps at `--dialog-content-width` and shares the dialog-input recipe. Hierarchy (round G): member icon 40px = loose-row title = group-head title / bucket-star (24px indent + faint accent connection line); selecting keeps checkboxes on the 8px axis with a 36px anchor step (icon 64px). Perf: staging-only actions repaint ONLY banner+toolbar+`#staging-items` (`renderStagingNow`, anchored at `.staging-cut`; recent-region nodes and their favicons untouched, `syncRecentStageButtons` updates the recent stage glyphs in place) and a clean re-activation updates just the bucket's new-N pill (`painted`/`lastRenderedRaw` guards) — verified in a real Chromium via `scripts/harness/diag/diag-staging-verify.js` (hierarchy/axis/indent/selection-checkbox-axis/send-button-axis/quick-tail/DnD/reorder/entry-churn=0) with `diag-staging-perf.js` and `diag-staging-geometry.js` as the reproduction probes. A guide strip above the toolbar (「不再提醒」 dismiss, `stagingGuideDismissed`) introduces the workbench; the shortcut editor defers its post-save render so a newly added chip receives keyboard focus (←/→ walk it immediately), its alias input caps at `--dialog-content-width`, and the picker opens at z-index 210 above the editor (pins/recents chips visible). Perf round H (master merge): the full repaint rides the 4.1.0 chunked painter (paintListChunked, pipes mode — banners/toolbars/empty <ul>s land with the head, staging rows stream in 60+120 adaptive batches, the recentCount-bounded recent rows land with the head; a pending paint is cancelled before every partial repaint), staging rows joined the content-visibility:auto roster, and the row loops hoist per-render i18n labels (stagingFromHistory/star/remove + recent bucket labels resolve once per render). Perf round I (fold surgery): group/bucket folds no longer repaint the staging area - toggleGroupFold/toggleBucketFold sync the head li in place and remove/re-insert only its contiguous .staging-member rows (recent region untouched, lastRenderedRaw advances so re-activation never back-repaints); dupes/tabgroups got the same surgery (foldGroupSurgically). Fold-memory round (perf round J): the idle toolbar became the REAL staging section head #staging-head (chevron + bold title + count pill moved into the right cluster with [new group][select mode], folding the whole staging area - headCollapsed persists in the model); the recent time sections became REAL foldable group heads (li.recent-group-li with chevron/title/count pill/stage button, member rows carry data-recent-group, recentGroupCollapsed persists per bucket, folds are surgical via recentMemberHtml/recentGroupRows); both big heads are 32px tall with 14px/600 titles; the selection fold snapshot covers the new folds; both counts are unified pills (staging aria = stagingCount, recent aria = title . count); the guide banner gained the universal session x (risk-banner law) beside the permanent dismiss; the assign dialog show rules (.needStagingGroupAssign display + cover) were added - the toolbar group button used to write the body class with no CSS to reveal the dialog; .staging-remove joins the 680px label tier; STAR_X_ICON/STAGE_REMOVE_ICON redrawn as full-size glyphs with a bottom-right corner x (no shrink). Instant-fold round L: the two big heads fold/unfold via root classes (staging-area-collapsed / recent-area-collapsed) - the recent region always paints (rows hidden not dropped, the fetch is never skipped), the staging area keeps the prebuilt stagingRowsCache and the unfold drops it in ONE innerHTML (measured 3.2ms sync / 19ms settled vs the old 150-250ms full repaint); viewRecent was re-translated across all 42 non-en locales (zh_CN had stayed 最近添加). Axis round K: member rows carry a .staging-connector span and the flat 2px accent edge line became a REAL tree drawing (the tabgroups color-line law, muted) - a 1px light DASHED trunk drops from the PARENT NODE'S ICON axis (the head glyph-well center, 34 narrow / 35 wide = --stg-trunk-x; 2026-08-27 re-drawing - it starts BELOW the icon, +12px past the row middle, never over the glyph), each member row gets a dashed tick to its favicon LEFT edge (48/50 = --stg-tick-w), the last member closes the trunk with an elbow (staging-last; has-members gates the head's half, collapsed heads hang no line; selection mode keeps its own checkbox-axis calibration, still chevron-anchored at 39.5 - there the head well and member favicons share one column so the icon axis has no room); dashes are a 2px-on/3px-off repeating gradient in a 30% muted token. Peer-heads + icon rounds (2026-08-25): the three fold heads are PEERS of their member rows — the title inherits the body base font at weight 400 (the old 11/12px/500 pins read smaller AND bolder than the row titles), line-height mirrors `.tree-item-link`'s 1.67em so heads ride min-height and the wide/panel form stretches every fold head to the two-line row height (3.34em, single-line content vertically centered — the creation-time/range `.head-sub` meta line shipped and was withdrawn the same day, its three locale keys deleted). Every fold head now leads with a GLYPH on the bucket star's slot: the tree's `FOLDER_ICON` on group heads, `CLOCK_ICON` on the recent time buckets — glyph left edge = the LOOSE row's TITLE column (40px; the star's old 2px lead parked it at 42, off its own documented column since inception) and the glyph y-center sits on the title ink center to ±0.02px (diag-verified narrow AND wide, where head height equals the two-line row to 0.02px). The recent time-bucket rows gained the staging-member 24px indent, so both areas share one hierarchy law: head glyph column = member favicon column (40px narrow / 64px selecting). The same round landed the selection-mode head alignment: the heads' first content glyph (group `.head-main` / bucket star) gains a 10px selecting-only inline lead so it stacks on the member rows' 64px first-glyph column (the long-claimed "64=64" was aspirational — the title sat at 54px, the star at 56px), and `diag-staging-verify.js` was recalibrated (bucket tail's rightmost is 移除暂存 not fav-all, chevron 18px fold lead, time-bucket title on the 40px axis) — the diag is ALL-PASS again. Unit-tested by `tests/view-recent.test.js` 2026-08-26 open×4 + named-group round: the group head quick tail reorders to SIX always-visible keys [open-all OPEN][open-as-tab-group TABS][rename][dissolve][place][remove] (open pair accent, drops off an empty manual group; ≤400px folds open-group/rename/dissolve); the recent time-bucket stage buttons and a new `api.addItemsToNamedGroup(name, entries)` land batches in a staging group NAMED after the origin (same-name group absorbs the append, `staging.findGroupByName`); `api.groupUrls`/`groupName` feed the group menu's open entries. Alignment round (diag-staging-geometry.js ALL-PASS, narrow+wide): member favicon left edge == the loose row's title axis, head glyph CENTER == member favicon center (the glyph rides the favicon's 20/22px well), head title == member title — the wide two-line form steps the head well +6px and the member indent 24→30px (a LATER cascade block — same specificity beats the narrow base only by order). Report round 3 (2026-08-26): the SAME three laws now hold in selection mode, re-anchored on the checkbox axis (diag-measured narrow 52/62/76 + wide 58/69/88): the member anchor margin steps 24/30px (was 36), the head glyph well re-docks to the member favicon start (the chevron's 12px tail margin is normal-mode chrome — the checkbox owns the selection lead) and the connector tick retargets the new favicon column; `syncRecentStageButtons` flips the FULL button (class + aria + labels + inner svg) through the shared `flipStageBtn` — a bucket-head send left never-sent rows accent-tinted but HOLLOW (`.staged` only re-colors; the solid-plane swap needs the icon innerHTML), verified by `diag/diag-recent-stage-flip.js`. Report round 6 (2026-08-26): the idle 暂存全部 send flips every result-row plane in place, and the history AREA shows only `searchHistoryCount` rows (options 搜索 → 最近搜索显示条数, default 5 — the render crops, the stored MRU cap is independent; the area height follows the cropped rows). Audit round (2026-08-26): the HEAD planes join the sync — bucket heads (`.recent-group-stage`) and the region's stage-all flip per the tabgroups group-head law (every member staged → `staged` + STAGE_ICON_DONE, aria-pressed; the send label/aria-name stay — heads are indicators, not toggles), and the sync now also runs on the FULL paint's settle tail (a first paint with everything already staged no longer renders hollow heads; the guard requires both querySelectorAll and querySelector so innerHTML-only test doubles skip quietly). | Report round 5 (2026-08-26): DISABLING THE VIEW (showRecentBookmarks off) is the same contract as the staging master switch — `api.isEnabled()` now requires the view to be enabled too, so every cross-view staging entry (relay buttons, stage-all toolbars, context-menu items) stands down with the hidden view Tree-law restack (2026-08-26, user call 暂存区层次照搬树视图): the workbench grid is now the TREE's exact three slots — heads [chevron @8..24][glyph well @24..44][title @48] (the head's own 4px flex gap is cancelled by a -4px chevron tail margin), loose rows reserve a 16px ::before slot so their favicon centers stack on the head glyph column (the tree folder-vs-bookmark rule), and member rows indent to 32px so their favicon LEFT edge lands on the head title axis 48 (TREE_INDENT child law; recent rows the same). The dashed trunk drops from the chevron center (16px) with ticks to 48. Selection-mode geometry was re-anchored 2026-08-29 (maintainer call): the small heads render NO chevron while selecting (folds are disabled there — dead chrome), the checkbox owns the lead and the head glyph well docks right after it (@32..52 narrow / 32..54 wide, center 42/43), and the dashed connector re-anchors to the HEAD ICON's center axis (trunk 41.5 narrow / 42.5 wide, ticks out to the member favicon left edge 52/58) — the same icon-axis law as normal mode, running in the clear gutter beside the member favicons (the round-3 chevron-anchored 39.5 calibration is retired; member anchor margins 24/30px are unchanged). diag/diag-staging-verify.js recalibrated + ALL-PASS; diag/diag-hierarchy-probe.js dumps the live rects. |
| `src/staging.js` | Staging pure data model (velvet staging §0.3/0.4): the `staging` local key's JSON shape — dual-state items (`id` non-null = bookmarked anchor, `null` = url/title snapshot; NO favorite flag — id IS the state), URL as the uniqueness key (resends never duplicate and never touch the existing row's group), 500-item hard cap (batches reject whole), groups (createdAt order, sourceFolderId/sourceTabGroup merge, dissolve forgets the source), the unbookmarked-inbox bucket derivation (id=null && group=null) with `lastSeenTs`/newCount, and the relink engine (dead anchor → another same-url node or fall back to id=null — items only leave through the explicit move/delete/remove/clear exits). Zero chrome/DOM — the view and actions layers own persistence and rendering. Workbench round (2026-08-23): groups carry a `manual` flag (user-built via the toolbar 新建分组 / the assign dialog's new-name path) — `pruneEmptyGroups` keeps them through an emptied member set (a manual group is a landing zone, not a leftover), `deleteGroup` removes group + members together and returns a receipt `restoreGroup` undoes (the view's confirm + toast path), and `reorderGroups(draggedId, targetId)` lands a dragged head BEFORE its target, rebasing `createdAt` ascending so the create-time sort invariant survives manual arrangement. Pre-release audit round (2026-08-24): `restoreItems(state, snaps, groups)` is the group-aware undo counterpart of removeByUrls/clearAll/dissolve — it re-creates missing groups FIRST (same ids, so member snapshots reattach; `add` cannot do this) then re-adds item snapshots verbatim, with whole-batch cap rejection (`{full:true}` the caller must surface). Unit-tested by `tests/staging.test.js` 2026-08-26: `findGroupByName(state, name)` — the exact-name landing lookup for the named-group batch sends (oldest match wins, blanks never match) |
| `src/staging-relay.js` | The staging relay's shared row-button recipe (2026-08 relay round, spec in docs/plan-velvet/velvet-feat-staging.md §13): `stageBtnHtml(api, item, _m)` (the hover 发送到暂存 toggle — `.staging-add-btn` + `.staged` accent + STAGE_ICON/STAGE_ICON_DONE, the stats-view §2.3 original extracted), `toggleStageItem(api, item)` (one click = one toggle: unstaged → addItems, staged → removeByUrl; returns the new state or null when unavailable), `flipStageBtn(btn, staged, _m)` (in-place icon/label/class swap — the hosting views do NOT re-render on staging changes, so the click must flip its own button), and `isStagedUrl(api, url)`. Consumers: view-dead rows+toolbar, view-dupes rows/group heads/toolbar, search rows. The dupes group heads wrap it with whole-membership semantics (`stageGroupByKey` + `flipGroupBlock`). Zero state of its own; the api is view-recent's, passed as ctx.staging/ctx.stagingApi. The whole relay stands down behind the stagingEnabled master switch (options 暂存和最近添加, SYNC key): each host view gates its builders on api.isEnabled(), view-recent collapses to the bare recently-added list, and context-menu.js hides every staging entry at open time. Consumers since the fourth relay round also include the TREE rows (tree-render's meta.tailHtml quick actions [编辑][发送到暂存][删除], the treeRowActions options switch) |
| `src/toolbar-fit.js` | Selection action-rung label fitting (extracted from view-recent 2026-08): `fitToolbarLabels(bar)` reveals the iconified batch buttons' text labels ONE BY ONE from the right edge as free width allows (measured per render — no container breakpoints), `watchToolbarFit(listEl, fit)` re-fits on width changes via one ResizeObserver. Buttons opt in with `.vbm-fit-btn` + `.vbm-fit-label`; consumers: view-recent (staging actions rung), view-dead + view-stats (the two-rung selecting bars, fifth fix round) |

| `src/folder-pick.js` | Folder-picker quick-pick roster logic (velvet staging §4.1), pure: `folderPickPins` (user-ordered pin list) + `folderPickRecents` (LRU ≤6) helpers — tolerant parse, dedupe-move-front record, pin toggle, lazy dead-id pruning and the chips model (pins first, recents minus pinned). Unit-tested by `tests/folder-pick.test.js` |
| `src/clipboard.js` | Clipboard writes + folder-list formatting (velvet staging §6), pure: `writeText` (navigator.clipboard with the textarea+execCommand fallback — extracted from actions.js), and the three formatters (`formatAsText` two-line entries, `formatAsMarkdown` `- [t](u)` with bracket/paren escaping and newline folding, `formatAsJson` flat 2-space array) behind `FORMATTERS`. No dedupe — lists stay faithful to the tree. Unit-tested by `tests/clipboard.test.js` |
| `src/view-dupes.js` | Duplicates view (slice C): collapsible groups (group head = normalized URL + count pill + clean-rest), keeper radios with six `pickKeeper` strategies + scope (all/bookmark-bar) + `dupesIgnoreScheme` toolbar controls, will-delete strikethrough preview, ConfirmDialog-gated serial deletions through `undo.capture` + a single summary toast, context-menu/K-key keeper pin, `R` reveal. Slice D: keep-most-visited reads real `visitStats` counts and greys out while `statsEnabled` is off. Final polish: a member row inside an expanded group takes `Enter`/`Space` = open that copy (modifier-aware, via a synthesized click) and `←` = focus the group head (RTL-mirrored). v4 task-4: the group head's quick action is now a ✓ "apply this group's dedup" with a strategy-naming hint ("keep 〈title〉, remove the other N") — the old × glyph read as a plain delete and went unnoticed (#9), selection-mode Space toggles the focused row's checkbox everywhere (#8), and the version-gated risk banner (`src/risk-banner.js`, #14) shows before the first cleanup. 4.0.1 focus law: render() parks/restores the focused row across the innerHTML swap (same-id replacement, index-clamped fallback, container fallback when empty; shared via `src/list-focus.js` since 4.0.5, the toolbar focus park/restore included — this view's risk-banner controls ride along via `TOOLBAR_CONTROLS_SEL`) — a menu action that re-renders (e.g. "set as keeper") no longer loses keyboard focus. 4.0.5 hardening: the serial deletion chain reads `chrome.runtime.lastError` per remove and the summary toast reports the ACTUAL removed count; applying a selection exits selection mode; the confirm dialogs name the keeper (`dupesConfirmGroup` "Keep \"$title$\"…", strategy-neutral `dupesConfirmAll`) and every batch confirm appends the one-step-undo note (`undoSingleStepNote`). 4.0.8: the idle toolbar is two aligned `.vbm-toolbar` rows (`.dupes-controls-toolbar` strategy/scope/scheme; `.dupes-actions-toolbar` summary/apply-all/select) with an icon-only select-mode entry, and member rows carry a hover/focus-revealed per-member delete (through `actions.deleteBookmark`, so it is undo-captured) that regroups live — a 2-member group dissolves, a deleted pinned keeper falls back to the strategy pick. 4.1.0 perf: render() streams group blocks through `src/list-chunks.js` (banner + toolbar + first groups synchronously, the rest in rAF batches; with the 4.1.0 实验室 `virtualScrollLab` switch on it paints through `src/virtual-list.js` instead — same callbacks, a storage listener re-renders on the live toggle) — a 2508-row regroup used to freeze the popup for the whole ~470 ms parse; the head/member focus restores and the row park/restore retry per batch (their targets may sit in any chunk) and always run at settle. 4.1.0 perf round 2: member rows resolve their `_m()` labels from a per-render dict (4 keep/delete crossings per row × 5000 members = 20000 getMessage calls eliminated); render() records the piece index of every group head and member so `onChunk` retries focus restores ONLY once the target's piece is in the DOM (no per-batch `querySelectorAll('li.dupes-group')` scans), and the chunked paint runs adaptive (budget 16ms, 24-240 pieces). 4.1.0 fold surgery (perf round I): a group fold no longer re-runs refresh() (getTree -> flatten -> regroup -> cache -> full repaint - ~530 ms before the first frame at 1920 rows); foldGroupSurgically toggles aria-expanded/chevron on the SAME head li and removes/re-inserts only the group's own .dupes-member rows via groupMembersHtml (the virtual-lab painter and unit doubles keep the full render path). Axis round K: the member geometry mirrors the staging workbench axes - the keeper radio's center lands on the staging loose row's icon center (26px, li lead 8->18px) and the member icon's center lands on the staging grouped member's icon center (50px, a::before 0->6px so the icon slot starts at 40px); diag-axis-align.js asserts both axes pixel-exact. Fold-surgery rounds 2-3: collapse detaches the group's .dupes-member nodes into a WeakMap-keyed DocumentFragment and expand reinserts the ORIGINAL nodes (no HTML rebuild, no favicon load storm, no dead-overlay rescan — onRowsRendered runs only on the HTML fallback); each group renders as head piece + members piece and under the virtual painter a fold goes through the painter's fold() piece-range API (foldVirtual — collapsed groups still build their members piece, painted hidden via hiddenRanges, so expands stay surgical; falls back to refresh() on non-geometry doubles).  Relay round (2026-08): member rows + group heads + the actions toolbar carry the 发送到暂存 relay (`src/staging-relay.js`) — the toolbar plane sits left of 全部应用 (all members, keeper + doomed alike), the head plane left of the ✓ apply (whole-membership toggle via `stageGroupByKey`, exposed on the api for the `dupes-group-stage` menu entry), member planes left of the per-row delete; flips land in place. Unit-tested by `tests/view-dupes.test.js` 2026-08-27: the group-head fold indicator joined the small-head tree-twisty tier (CHEVRON_ICON SVG, rotate/scaleX laws in neat.css's shared block) |
| `src/view-tabgroups.js` | Tab-groups view (4.1.0, docs/plan-4.1.0/tab-groups-view-design.md): the seventh view, registered between search and recent — every window's tabs in real order, grouped tabs nested under their Chrome tab group (title/color/member count, group heads fold like tree folders with the same arrow model), ungrouped tabs as plain rows, the current tab flagged, pinned/discarded state badges. Group actions: rename/activate/close/sleep/wake/ungroup/move-to-new-window/save-as-bookmark-folder (color + title remembered, restored by *open as tab group*); tab actions: activate/pin/sleep/close/bookmark (into the quick-add `quickAddFolderId` folder). Selection mode batches group-new/open-into-existing/close/sleep with a copy-vs-move dialog for already-grouped tabs; a *recently closed* section (persisted `tabGroupsClosed`, depth `tabGroupsClosedLimit` 5–50, cap-10-style) reopens whole groups or restores/bookmarks/forgets single tabs. Four dedicated context menus (tab row / group head / closed group / closed tab) ride the shared menu + keyboard machinery; collapse state persists per session (`tabGroupsViewState`, local); the optional browser collapse-sync (`tabGroupsSyncCollapse`) and group-color style (`tabGroupsColorStyle` off/edge/line — **line is the default since the 4.1.0 polish round** (the legacy boolean `tabGroupsColorBorder` still reads as edge; an explicit off wins) live in the new options *Tab groups* group. 2026-08-22 audit round (docs/review-4.1.0/tabgroups-audit.md): activation focuses the owning WINDOW too (a cross-window click used to look dead); the first activation of a session scrolls the current tab into view (`block:'nearest'`); the toolbar carries an instant title/URL filter (folds inert + fold buttons disabled while active, Esc clears, selection mode clears, session-only, closed records out of scope) with a trailing-edge clear × once it holds text (the `#search-field` recipe); `tabGroupFolderMeta` prunes against the live folder set on every refresh and sits in `KNOWN_KEYS` + the storage census; Delete on structural rows is swallowed in-view (never falls through to bookmark delete). 2026-08-22 UX pass (the 12-point review): SINGLE-tab/group close & sleep run WITHOUT ConfirmDialog — the toast is the regret path (`toastAction`: Reopen → `restoreClosedGroup`, Wake → wake; batch close/sleep still confirm, and a selection of exactly one routes through the single-op path); deleteClosedGroup/removeClosedTab/clearClosedGroups and both save-as-folder flows carry toast Undo (the folder undo is capture-then-`removeTree`, re-undoable via the bookmark stack); the row's pin column renders a hover pin button for unpinned tabs (pinning was context-menu-only) and the hover-reveal CSS keys on `:has(:focus-visible)` so a mouse click never pins the strip open; the group head's shared tail reorders to sleep → save (FOLDER_STAR icon) → close, column-aligned with the rows' [sleep][star][close]; closed-group heads take the live head's dot margin and their expanded members the 24px member indent (`tabgroups-closed-member`); the current-window pill keys on `currentWindowId` (always exactly one window marked) and takes the current-tab badge's accent style + right-zone position; the current-tab row gets an accent 8% tint (`li.tabgroups-current`); clear-all lives on the "recently closed" section head (`.vbm-section-head` — the button joins the Tab ring after the row stop and exchanges focus between the open-tab and closed zones via ↑/↓); closed-record heads answer Space/Enter/←/→ with the standard fold protocol; view-tab badges hide below a 56px per-tab container and the current-tab text pill below a 400px popup container (width-driven, independent of `showTabBadges`). Follow-up polish round: pinning a grouped tab toasts `tabGroupsPinnedUngroupedToast` (the silent group-exit was invisible); batch close writes ONE merged closed record (`cb_` id, `tabGroupsBatchClosedTitle`) with the same toast Reopen — the 10-slot history no longer depends on N singles; the instant filter matches GROUP titles too (a name hit shows the whole group, Chrome-tab-search parity); `restoreClosedGroup` sends the record's `windowId` with `vbm-tab-group-open-new` (SW validates, stale ids degrade to the default window) and opens single-TAB records as plain background tabs in their window (the openNew trap used to resurrect one tab as a titled group); the standalone closed record leads with a reopen arrow (the ☆ stays on member rows + the context menu) and the whole history strip is single-line — top-level entries at the head height (28px), members at the data-row height, absolute time in tooltips; ≤400px the group head's activate/rename buttons collapse (F2 + the → menu keep them). Window heads carry ONE hover action — close the whole window (confirm on the tab count, ONE merged `cw_` record titled via `tabGroupsWindowClosedTitle`, toast Reopen; Chrome's native Ctrl+Shift+T remains the whole-window regret). The head's keydown handler passes buttons through (native Space/Enter click, no fold toggle). Registration follows the 4.0.8 contract: `showKey: 'showTabGroupsView'` + `disableKey: 'disableTabGroupsView'` (options-page toggle + view-tab Hide/Disable + live storage sync via view-manager), `typeAhead: false`, badge = tab count, `onEscape` exits selection mode. 4.1.0 P1 收尾 (H9 后续): the event debounce is split — inactive only `tabs.onCreated`/`onRemoved` refresh the count badge at a 1 s cadence, while onMoved/onUpdated/onActivated/onAttached/onDetached/tabGroups/bookmarks events re-render only the ACTIVE view at the original 300 ms. 4.1.0 perf (diag-41x-perf.js 实测驱动, the "点开要等好几秒" report at 1200 tabs / 160 groups / 6000 bookmarks): refresh() fires windows.getAll + tabGroups.query + bookmarks.getTree CONCURRENTLY (the old serial chain cost three IPC round trips, ~100 ms combined); the bookmark-tree walk is dirty-marked — bookmarks events set `bookmarksDirty`, tab-only churn (the common storm) reuses the cached url/folder sets instead of re-walking a 6000-node tree (53-106 ms per refresh); group/tab lookups go through String-id Maps rebuilt with the arrays (the old per-call `array.find` made every grouped row O(groups) and the render walk O(tabs×groups)); group members are pre-bucketed in one pass per window (the walk used to re-filter+re-sort the whole window per group); the per-row `_m()` labels resolve once per render (7+ i18n crossings per row measured ~37 ms per 1371-row render); and render() streams rows through `src/list-chunks.js` (toolbar + first 80 rows synchronously, the rest in 160-row rAF batches) — the old single innerHTML put ~1.7 s of parse+style+layout on the main thread in one bite; with the 4.1.0 实验室 `virtualScrollLab` switch on, the same render paints through `src/virtual-list.js` instead (viewport window + paddings), keying the initial window to the current tab's piece (revealIndex) and following the live options toggle through the storage listener. 4.1.0 perf round 2: group-head labels (activate/rename/save/sleep/wake/close/untitled) resolve once per render (~800 getMessage crossings at 160 groups → constants); the current-tab scroll retry is piece-index gated (no per-batch querySelector walk); the chunked paint runs adaptive (budget 16ms, 48-320 rows). 4.1.0 fold surgery (perf round I): setGroupCollapsed no longer full-renders - foldGroupSurgically updates the group li in place (aria-expanded/chevron) and moves only its own contiguous li.vbm-row[data-group-id] members (window heads / closed sections untouched; the virtual-lab painter keeps the full render); browser collapse-sync write-through and persistUIState unchanged; round L adds window-fold surgery (foldWindowSurgically captures the block AS PAINTED - group folds included - and re-inserts it, the virtual-lab painter keeps the full render). staging §2.5 (user-revised): the tab row / closed-tab row / group-head menus "Send to staging" entries are PURE snapshots (id-null url/title, zero bookmark writes — the group entry batches into a sourceTabGroup group, >10 confirm, 0-bookmarkable toast); the classic ★ addTabToBookmarks flow is untouched. Fold-surgery rounds 2-3: group/window folds stash the member nodes (WeakMap per head) and expand reinserts the ORIGINAL nodes (window folds too — the outerHTML serialize/reparse and windowBlockCache are gone); under the virtual painter both fold kinds route through the painter's fold() piece-range API (group bodies are their own pieces; a collapsed window's block is still built and painted hidden so unfolding stays surgical; falls back to render() on non-geometry doubles). Unit-tested by `tests/view-tabgroups.test.js` 2026-08-26 window round dropped the 12px title pin; the 2026-08-27 big-head round went further - the window head title now speaks the staging big heads' language (14px/600) while its chevron keeps the UNPINNED body size, the staging/recent big heads' 12px container pins retired so all three big-head chevrons share one body→view chain (identical by construction), and a -6px tail margin lands the title on the icon-column left edge 24; the toolbar gains a 新建窗口 icon (WINDOW_PLUS, left of 刷新 — `chrome.windows.create`); the tab-row menu gains 移动到新窗口 (`moveTabToNewWindow`, the same `tabsMoveNewWindow` SW message with one id); and the window HEAD is an HTML5 drag source — dropping it on any row/head of ANOTHER window sends `tabsMergeWindow` (whole tab set moves; the non-pinned subset becomes one titled 窗口 N group; the emptied source window closes by itself, toast-only farewell; the target section highlights via `.window-drop-target`). The merge path is e2e-verified by `diag/diag-tg-window-merge.js`. Report round 2 (2026-08-26): the 新建窗口 button and `moveTabsToNewWindow` create the window with `focused:false` (no foreground steal), every cross-window move (merge + tab drags) re-focuses the popup's own window after the move lands (`refocusPopupWindow`, captured post-move from the send/drop callbacks), and a TAB drag now accepts a WINDOW HEAD as drop target — the tab appends to that window's END (`index:-1`); both head-drop paths are unit-tested and the focus/appends are asserted by the diag. Report round 4 (2026-08-26): `stageTabGroup` lands the whole group in a staging group NAMED after the tab group (`api.addItemsToNamedGroup` — same-name absorbs, else a fresh named group), and its groupId match is String-safe (chrome.tabs populate returns NUMBER groupIds vs the DOM dataset string — the strict === sent every group into the 'no bookmarkable tabs' toast); e2e-verified by `diag/diag-tg-group-stage.js`; every batch send flips its rows' planes in place (group head / closed group head / closed tab — the window-head send's law, diag-asserted solid). Tree-law restack (2026-08-26): the group head joins the tree grid — [chevron @8..24][dot WELL 20px @24..44 centering the 10px color dot on the level-0 icon column][title @48], so the dot's center == the ungrouped tabs' favicon center below it (previously the bare dot rode the chevron's tail at center 29 vs 26); ungrouped rows gain an 8px lead (favicon column 24..44) and grouped member rows indent 24→32px so their favicon LEFT edge lands on the group title axis 48 (TREE_INDENT child law). The color-line trunk moves to the dot's new center (--tg-line-x 32.5) with the tick running to 48 (--tg-tick-w 15.5); the window head keeps its own chevron on the SAME 8..24 trunk column, so no extra window indent was needed. Selection-mode constants untouched. The recently-closed section joined the same grid in the same round: the closed dot rule was deleted in favor of the shared #tabgroups-list well recipe, standalone closed tabs took the 8px lead (icon column 24..44) and closed members 24→32px (favicon left edge == closed head title axis 48) — all probe-verified with a seeded tabGroupsClosed record. Verified by diag/diag-hierarchy-probe.js (window chevron == group chevron == 8..24; dot well == favicon column; closed members at 48). 2026-08-27 fold-indicator tiers: ONLY the window head keeps the solid ▾/▸ triangle (the big-head tier with staging/#recent-head); the group and closed-group heads carry the tree's CHEVRON_ICON twisty SVG (rotate 90° open, scaleX(-1) RTL, the #tree .twisty transitions). 2026-08-28 drag-refit round: a mid-drag auto-scroll re-windows under the virtual painter and rebuilds every row node — onChunk now re-applies the drag session's visual state from the closure ids (refitDragMarks: the .dragging source row by data-tab-id / the window head by data-window-id, plus setWindowDropTarget(wid, force) re-marking the merge target section; the drop itself always read dataset off the node under the cursor, and the per-row .drag-over comes back with the browser's next dragover — neither needed changing) | 2026-08-29 title-churn report (sites animating document.title flickered the WHOLE view at the flip rate — every flip was a 300 ms-debounced full refresh + innerHTML rebuild): tabs.onUpdated is now triaged — a changeInfo whose keys are ALL in {title, favIconUrl, status} patches the ONE affected row in place (memory mirror on both the windows[] originals and the tabMap copies, then the li is regenerated and swapped via `src/tabgroups-patch.js` replaceTabRowNode, carrying the drag runtime classes and DOM focus, and patching inside fold stashes too — unfold reinserts ORIGINAL nodes, so stashed rows must not go stale); same-title repeats and favIconUrl/status-only flips are swallowed whole (no refresh even scheduled); every other field keeps the 300 ms debounced full refresh, as do all states where surgery is unsafe (preconditions mirror the fold surgery: active view, no filter text, no selection mode, no virtual painter, paint settled, no drag in flight — a failed/absent row swap self-heals on the next refresh because the digest below saw the title). refresh() additionally digest-gates the render: renderInputsFingerprint over EVERY render input (windows/tabs rendered fields, groups, folds, selection, filter, closed records, colorStyle/virtual/relay, per-row staged probe, bookmarksRev) — identical inputs paint NOTHING (badge still updates, IPC reads already ran); non-rendered tab fields (favIconUrl/status/audible) are excluded so such storms digest equal; view entry (forceNextRender) and the first-activation scroll always paint, and bookmarkedUrls mutations bump bookmarksRev so star flips re-render |
| `src/dropdown.js` | Shared custom dropdown (4.0.1 select replacement): a native `<select>` hands its open-state keys to the browser, so the dupes toolbar's strategy/scope controls are this component instead — a trigger `<button>` plus a hidden `<ul role="listbox">`, one keyboard protocol for every dropdown: on the closed trigger `↓`/`Enter`/`Space` open (`↑` NOT intercepted — the rung law walks it away); in the open list `↑`/`↓`/`Home`/`End` navigate (greyed options skipped, Page* swallowed), `→` (RTL `←`)/`Enter`/`Space` pick, `←` (RTL `→`)/`Esc` cancel, and `Tab` picks + closes then lets the Tab ring step on (a greyed current option closes WITHOUT picking). A window-capture `Esc` closes an open list before every other Esc layer, and document `focusout`/`mousedown` close it on focus loss — one mechanism covering palette/dialog opens, view switches and outside clicks, so a listbox never floats over a higher layer or outlives its view. `initDropdowns(container, { onSelect, rtl })` returns `{ closeOpen() }`. Unit-tested by `tests/dropdown.test.js` |
| `src/view-dead.js` | Dead-link view (slice C): cached-last-scan rendering (`deadLastScan`), executable start-hint empty state, live progress bar, dual-channel checks via `checkUrlDual` (`deadProxyServer`/`deadScanConcurrency`/`deadScanTimeout` settings — the legacy `deadProxyTemplate` relay setting is retired, dropped by a one-time store.js migration), dead/blocked status badges with a dead·blocked summary line, three-state result filter (persisted as `deadFilter`), row/bulk dead marks (`deadMarks`, ConfirmDialog-gated mark-all/unmark-all) with the cross-view red × favicon overlay (`#tree`/`#results`/`#recent-list`, re-laid after every tree rebuild). Batch deletion (4.0.1, 4.0.8 低优先级收尾): the toolbar delete-all removes every VISIBLE row — under the All filter that is `selectableRows()` = result rows plus the uncovered-mark residue, the same scope as selection-mode's select-all (the confirm names the filter scope and warns that All includes blocked rows and that Undo is one-step) — and selection mode adds a delete-selected; both run a serial `chrome.bookmarks.remove` chain (lastError read, the single summary toast reports the ACTUAL removed count), and Undo restores only the most recent deletion. The tab badge is the last scan's dead+blocked count (NOT the marks count), and bookmark `onRemoved` synchronously prunes `deadLastScan.results` so the badge never inflates or resurrects. The returned API gains `preloadBadge` (4.0.8 fix, wired by neat.js's idle badge-preload block): a render-free startup fold that reads `deadLastScan` + the live blob, joins the cached verdicts against the tree and calls `views.updateBadges()` — the tab badge's inputs are async and previously only `activate()` loaded them, so a popup open in another view left the dead badge dark until the view's first visit. 4.0.8 收尾: the four bookmark events re-join the tree and repaint even without a scan cache (the marks-only 上次标注 view used to keep removed rows as zombies whose ⚑ could re-persist a deleted id), and a finished scan (`onCacheWritten`) re-joins against the live tree before repainting, so a bookmark deleted mid-scan never resurfaces as a ghost row. 4.0.8 低优先级收尾: rows without a per-row scan `ts` sort last under detection-time ordering; a marks-only view whose marks are hidden by the unmarked filter shows the try-another-segment hint instead of the first-scan CTA; imported `deadMarks`/`deadMarkTimes` changes re-render an already-open side panel via `storage.onChanged`; the second toolbar's "All" reads "Any status" to disambiguate its counter from the category toolbar. The `.dead-proxy-strip` row above the toolbar gives one-click proxy management: with no server an add button plus a dismissable nudge when a finished scan has direct-failing rows (its × hides only the hint — the options page's strip checkbox re-arms it), with a saved server an always-visible manage chip + change/remove, and an inline add panel that validates the address, verifies the install-time `proxy` permission (contains + a never-prompting request fallback), refuses servers another extension controls, probes reachability and persists ONLY a reachable one; scans install the marker-PAC session through the same gate (failures degrade to direct-only and show on the chip) and tear it down on settle/cancel/pagehide, with a `vbmProxySession` storage.session marker for the SW's crash sweep. Final polish: the filter seg buttons rove with `←`/`→`, and the toolbar carries `.vbm-toolbar` so its controls join the Tab cycle. v4 task-4: the scan itself moved into the service worker (`src/dead-scan-sw.js`, #16) — this view is a storage mirror of the `vbmDeadScan` live blob (start/pause/resume/cancel are fire-and-forget messages) and of the `deadLastScan` snapshot; mid-scan repaints are silent, preserving scrollTop and focus (#17), and the progress label is the bare `done/total` counter with the full sentence in title/aria (#17); the idle state stacks THREE icon `.vbm-toolbar` rungs below the proxy strip — detection (last-scan time/rescan/clear + verdict filter) / marks (mark-all/unmark-all/delete/select) / status (mark filter + sort), every icon button titled, right edges column-aligned, the clear-marks glyph a solid flag — each its own arrow rung in visual order (#13, keyboard-model §2.5); a version-gated risk banner (`src/risk-banner.js`, #14) shows before the first cleanup. 4.0.1 focus law: render() parks the focused row before the innerHTML swap and restores it after (same-id replacement, index-clamped fallback, container fallback when empty; shared via `src/list-focus.js` since 4.0.5, the toolbar focus park/restore included — this view's risk-banner controls ride along via `TOOLBAR_CONTROLS_SEL`). 4.0.5 hardening: `bookmarks.onCreated` also schedules a re-join (undo-restored / mid-session new bookmarks); the tab badge derives from the tree-joined result rows (`allResultRows().length`) and the cold-start preload block is gone — a stale cache can no longer revive an old count (the 4.0.8 `preloadBadge` startup fold above lights the badge at open, superseding the old first-activation-only behavior); mark-all / select-mode visibility keys off the FILTERED row count like delete-all does; every batch-delete confirm appends the one-step-undo note (`undoSingleStepNote`). Marks survive a cancelled scan (they are persistent user intent, not scan output) — with no scan result list on screen the marked rows render as their own list (joined to the tree, each individually unmarkable, a `deadMarkedCount` section header) ahead of the start hint; with a cached result list on screen, only the marks that list does NOT cover (a marked id the last scan never probed) get a supplementary list, so a cached scan never strands a mark either and covered marks are not duplicated. Aborting a run never strands a marked id with no way to clear it. 4.1.0 perf round 2 (report docs/review-4.1.0/perf-round2-audit.md): while a run lives, each 700ms blob publish no longer rebuilds the whole list — render() takes the INCREMENTAL path (toolbar patched in place with focus park/restore; only NEW settled problem rows are parsed and inserted at their tree-order position inside the SAME results `<ul>`, tracked by `liveDom.{ts,ul,rendered,markedIds,markedCount}`), so already-painted rows and their `_favicon` `<img>`s survive every tick (the old path re-created them all per publish — O(n²/scan) and icon flicker/refetch storms); a mark-move, run change, selection mode or capability-less double falls back to the full paint automatically, and the live first paint stays a single shot so the incremental state covers EXACTLY the painted rows. Idle renders (filter/sort switches, scan-finish settle, batch marks) stream through `src/list-chunks.js` pipes — results `<ul>` + marked-residue `<ul>` from one head paint (60 first / 100 per batch, adaptive 16ms budget) — instead of one giant innerHTML parse; string-model test doubles keep the old full-string build (rows INSIDE their ul — the D1 lesson: only the shared painter may chunk). Row labels resolve from a per-render dict (6-10 getMessage crossings per row eliminated).  Relay round (2026-08): every result/residue row carries the hover 发送到暂存 toggle (`src/staging-relay.js`, between the ⚑ mark and the × delete) and the marks toolbar gains a `dead-stage-all` plane left of delete-all (every FILTERED row joins the workbench; the visible planes flip in place). Toolbar polish (2026-08-25): 标记全部 moved INTO the right-pinned marks cluster, directly left of 取消全部标记 (it used to render loose beside the filter segment and the pair drifted apart whenever the toolbar had slack), and the row-1 danger trio (重新扫描/清除结果/删除全部) needs cluster-safe selectors — the `.dead-icon-cluster` base rule (0,3,1) outranked the old (0,2,0) danger rules and repainted the trio accent (the design-laws §2 specificity trap). Unit-tested by `tests/view-dead.test.js` |
| `src/visit-stats.js` | Visit-stats store (slice D), pure logic: `{ [bookmarkId]: { c, t } }` under the single `visitStats` key, 500 ms debounced persistence (`flush()` for tests), `record/get/countOf/all/prune/clear/enabled`. `statsEnabled` off = zero writes (record and prune both stand down; `clear` always works — the erasure exit must stay reachable). `prune(validIds)` runs on every tree rebuild so the dataset tracks the live tree. Unit-tested by `tests/visit-stats.test.js` |
| `src/view-stats.js` | Stats view (slice D): one row per bookmark with recorded opens, count pill + relative time (the active sort key sticks to the title column), sort segmented switch persisted as `statsSort` (count default), ConfirmDialog-gated "clear statistics" toolbar button, `statsEmpty` / `statsDisabledHint` empty states. 4.0.1 merged list: bookmarked history rows the stats dataset lacks merge into the stats rows (counted at history's visitCount), and the latest unbookmarked `chrome.history` entries join the same `<ul>` while the persisted `statsShowUnbookmarked` toolbar checkbox is on — bookmarked rows carry an inline ★ at the line end, unbookmarked ones a hover ☆ one-click-bookmark button that dedupes by URL before `chrome.bookmarks.create`. Rows open through the shared `bookmarkHandler`, which bumps the row's own counter via the `onOpenBookmark` hook. Final polish: the sort seg buttons rove with `←`/`→`, and the toolbar carries `.vbm-toolbar` so its controls join the Tab cycle. 4.0.1 focus law: render() parks/restores the focused row across the innerHTML swap (same-id replacement, index-clamped fallback, container fallback when empty — the mechanism is shared via `src/list-focus.js` since 4.0.5, the toolbar focus trio included). Velvet staging §3.7: selection mode (unit = url across BOTH row kinds — bookmarked and history rows select alike; actions degrade to what applies: stage-to-staging with ids/nulls, open, open-in-group, delete-bookmarked-only with confirm+undo; Space/Delete capture keys, Esc via the view onEscape). Perf round 3 (2026-08-24): the populated list renders through paintListChunked (toolbar + first 80 rows synchronously, the rest in adaptive 16ms-budget batches; empty/disabled/guide-only shapes keep the single paint) with piece-index-gated pendingRowFocus retries and toolbar/row focus park-restore across the chunked swap. Unit-tested by `tests/view-stats.test.js` |
| `src/visit-stats-sw.js` | Service-worker visit collector (slice E): exact-match `chrome.tabs.onUpdated` URL navigations against a bookmark-URL index (rebuilt LAZILY — P1-3: no eager cold-start `bookmarks.getTree`, the index builds on the first URL navigation or the first bookmark event via an `indexReady` gate; `onMoved` is debounced 300 ms so a recursive folder sort's move storm collapses into one rebuild), debounced read-modify-write merge into `visitStats`, `statsEnabled` zero-write contract, and the popup-open dedupe protocol (page side stamps `vbmPopupOpens` url→ts in `chrome.storage.session` via the exported `markPopupOpen(url)`; fresh markers make the collector skip). Started top-level by `src/background.js`. Unit-tested by `tests/visit-stats-sw.test.js` |
| `src/idle.js` | Idle-deferral helper (4.1.0 P1-2, docs/plan-4.1.0/build-and-performance-plan.md §4.4): `deferIdle(fn, { timeout })` runs non-critical startup work after the first render — `requestIdleCallback` with a hard timeout, falling back to `setTimeout 0` — plus a `?perf=1`-gated `mark(name)` that records `vbm:<name>` `performance.mark` points for DevTools first-paint profiling (no-op without the param). neat.js uses it for the remote announce fetch, the favicon-enrich storage hydrate (`deferHydrate`) and the four startup badge preloads (stats/dupes/tabgroups refreshes + the dead view's render-free `preloadBadge`). Unit-tested by `tests/idle.test.js` |
| `src/undo.js` | Deletion undo (P3.3): subtree-snapshot undo stack persisted to `chrome.storage.session` (`vbmUndoStack`, max 10, in-memory fallback when session storage is unavailable). `capture(id)` snapshots via `getSubTree` **before** a delete; `undo()` recreates depth-first at the original `parentId`/`index` then fires `onChanged`. Also owns the `#undo-toast` bar (8 s auto-hide, Undo button) shown by the actions delete flows instead of the old ConfirmDialog — generalized in v4 task-3 #14 into `toastAction(message, buttonLabel, onAction)`, a one-shot custom-action toast (the tree view's outside-the-bar reveal hint uses it; a later `showToast`/`toastAction` or the auto-hide clears the pending action). 4.0.8: a falsy `buttonLabel` renders the toast BUTTONLESS (`toastButton.hidden`, `pendingAction` forced null, the click listener early-returns while the button is hidden) — the view-manager hidden-view return hint rides this form, and `showToast` always re-shows the button with the Undo label. `initUndo(ctx{ onChanged })` returns `{ capture, undo, canUndo, peek, showToast, toastAction }`. Unit-tested by `tests/undo.test.js` |
| `src/palette.js` | ⌘K command palette (P2; v4 task-2 slice C retired the `mode: dupes/dead` state machine — those modes became the dupes/dead views): overlay (`#command-palette`, markup lives in both popup pages) unifying fuzzy bookmark search, folder jump (`treeView.revealFolder`) and a curated slash command table (v4 task-4 #5, round-5 — 20 entries: create-style `add`/`new`/`folder`, `/session` (keepOpen), one Go per view (`tree`/`search`/`tabgroups` (4.1.0)/`recent`/`stats`/`dead`/`dupes`; `/search` is keepOpen and carries rest words into `search.run`), parameterized `/theme <name>` (unique-prefix match, bare `/theme` shows the usage AlertDialog) plus the four direct theme switches `/dark` `/light` `/ink` `/paper`, `/tabs`, `/version` (4.0.8 — closes the palette and opens dialogs.js's VersionDialog with extension/announce/browser metadata + a JSON copy button), `/lang <code>` (4.0.8, keepOpen — switches the UI language through `src/i18n-live.js` and reloads; an invalid code shows the usage alert and keeps the panel open), `/options`; the old `/path` and per-theme commands are gone; slash mode renders slash-prefix matches before name-fuzzy ones so the exact command wins Enter — `/ink` applies the Ink theme, not "Go to Dead links view"). Plus user-defined custom commands (v4 task-4 #6): rows carry a custom tag, join slash matching usage-ordered, execute via `executeCustom` from `src/palette-commands.js`, `→` opens their own edit/delete context menu (`#palette-cmd-context-menu`), and a hitless slash query ends in a save-as row that hands over to the options editor with the slash prefilled (`pages/options.html#palette-cmd={...}`). Toggled by Ctrl/Cmd+K (capture-phase, refused while dialogs are open) or globally via the `open-command-palette` command (background sets a `storage.session` `pendingPaletteOpen` flag then `chrome.action.openPopup`, falling back to a `?palette=1` popup window). Query synthesis: empty → commands only, `/`-prefixed → commands only (slash aliases prefix-match, e.g. `/d` → `/dead`+`/dupes`+`/dark`), otherwise commands first + `VBMFuzzy.rank(...).slice(0, 50)` + a bridge row (`paletteCmdSearchInView`, closes and runs the query in the search view); index rebuilt from `getTree` on every open. `open()` is a no-op when the `paletteEnabled` setting is off (v4 task-3 #20 classic-experience switch — covers Ctrl/Cmd+K, the tool button and the global command alike). 4.0.5: Tab is trapped inside the panel as a two-stop cycle — input ↔ the footer Esc close button (preventDefault keeps document tabCycle's native Tab from leaving the ring; both stops sit inside `#command-palette`, so the focusout guard no longer auto-closes the panel — the close button keeps `tabindex="-1"` in the markup and is reached only through this trap), and result-row anchors carry `tabindex="-1"` so they never join the Tab order (a tabbed-to row used to degrade ↑↓/Space into native scroll with the highlight frozen; a `$results` keydown guard is the safety net). Result titles render the matched characters with `<mark>` highlight (tree-render's `highlightTitlePositions`), an untitled folder row falls back to the `noTitle` label (the search view's folder-row contract), separators no longer enter the palette index (the same `SeparatorManager` check search.js's buildSearchIndex uses), the dialog-open guard reads `ctx.dialogs.anyOpen()` instead of a copied `DIALOG_CLASSES` list, and "a menu is open over the palette" now means the `.active` marker AND an actually visible menu (inline `opacity: '1'`) — `clearMenu()` hides every menu but keeps the marker, so treating the stale marker alone as open swallowed the first ←. 4.0.8: Go-to-view slash commands and custom view-preset commands follow view availability — hidden views keep their rows (entering toasts the return path), DISABLED views drop out and their save-as closure is suppressed; the palette input ignores IME-composition Enter like the search bar. `initPalette(ctx)` returns `{ open(), close(), isOpen(), customMenu }`. No neatools inside. Unit-tested by `tests/palette.test.js` |
| `src/palette-commands.js` | Custom palette commands (v4 task-4 #6, docs/palette-commands-design.md), pure ESM: the command model (`validateCommand` normalization + per-field errors), storage (`loadCustomCommands`/`saveCustomCommands` over the `paletteCustomCommands` sync key — a `store.syncKeys` member, so it rides the options-page backup too), matching (`matchCustom` prefix over slash+aliases, `sortCustoms` by useCount capped at 50 → lastUsedAt → slash), the runtime dispatch `executeCustom` (four action types: `open-url`, `url-template` (`%s` ← rest words, origin fallback when empty), `open-url-group` (folder → `actions.openBookmarks`; a vanished folder prompts a ConfirmDialog offering to delete the dead entry), `view-preset` (`views.activate(view, { preset })` with only the declared keys)) and `summarizeAction` for the management list. Unit-tested by `tests/palette-commands.test.js` |
| `src/options-palette-commands.js` | Options-page custom-command manager (v4 task-4 #6): the *Commands* group (`#palette-cmd-options`) — list with per-row edit/delete (delete via native `confirm()`), an add/edit form whose rows follow the action type (`.pc-for` visibility), and the `#palette-cmd={...}` hash prefill the palette's save-as row / edit menu navigates to. ES module separate from the classic `src/options.js`; awaits `store.ready` |
| `src/options-proxy.js` | Options-page dead-link proxy management (4.0.1 — the "Dead scan" group's proxy row, replacing the retired relay-template input): type an address, *Test & save* runs the same parse → `proxy` permission → controllability → reachability gate as the dead-links view's strip (all from `src/dead-proxy.js`) and persists only a reachable server as `deadProxyServer`; the row also shows the saved server and clears it, and the `dead-proxy-strip-visible` checkbox re-arms the view's dismissed no-proxy hint (`hideDeadProxyStrip`). ES module loaded after `src/store.js`, separate from the classic `src/options.js`. Unit-tested by `tests/options-proxy.test.js` |
| `src/custom-css.js` + `pages/custom-css.html` | 4.1.1 standalone custom-CSS workbench: the options page's inline #userstyle textarea (large pastes deformed the options layout) became an outbound link; this page owns the editor. MULTI-STYLE model (maintainer call): `userstyles` (local, JSON array of {id,name,desc,css,enabled}) is the source of truth; every change materializes the concatenation of ENABLED styles IN LIST ORDER into the legacy `userstyle` key — the cascade IS the conflict resolution (later overrides earlier), the apply side (userstyle.js) stays untouched, and a downgrade keeps the last effective CSS. Legacy migration: a pre-4.1.1 single userstyle becomes one enabled entry. Page UI (4.1.1 rework after maintainer feedback — the first cut's selectable list gave cramped half-width inputs and a label-in-clickable-row checkbox whose activation raced the row re-render, so unchecking via the label could swallow the click): TAB workbench — one tab per style (click = select only; a filled dot = enabled, hollow + dimmed = off; Arrow/Home/End key nav), ＋ mints a style, ◀/▶ on the strip's right end reorder the active tab in the cascade (moveStyle/pickNeighborId pure helpers), and the editor header carries the enable checkbox + full-width name + delete (danger, rightmost) with a full-width description input and the CSS editor filling the viewport (body padding/shadow dropped via `body:has(#custom-css-page)`; CodeMirror primary, native textarea fallback — `window.__vbmCustomCss` exposes a diagnostic api with an editor bridge since CM hides the textarea). No interactive control nests inside another. 4.1.1 rework round 2 (user report: long stylesheets had no scrollbar and overlapped the footer): the page boot gates on `store.ready` (reading the mirror at DOMContentLoaded could catch it empty and the unconditional boot persist would wipe `userstyles`), and the editor slot constrains CodeMirror properly — `min-height:0` releases the flex min-height:auto trap on the wrapper and `.CodeMirror-scroll` gets `height:100%`+`overflow-y:auto` (the vendored CM v2 css ships `height:auto`+`overflow-y:hidden`, which grew a 200-line paste to 2814px past the slot); the native textarea fallback scrolls by default. Pure helpers (parseStyles/migrateLegacy/materialize/newStyleId/moveStyle/pickNeighborId) exported for vitest. Unit-tested by `tests/custom-css.test.js` (incl. the uncheck-regression); e2e in diag-411-verify N1–N6 |
| `src/options-storage-badges.js` | Options-page storage-area badges: a cloud / crossed-cloud icon on every group header telling where that group's settings persist — `chrome.storage.sync` (members of store.js `SYNC_KEYS`) vs device-local `chrome.storage.local`. A MIXED group carries the majority kind on the header and only its differing rows get a right-aligned badge of their own (currently just General: `autoResizePopup`/`openInSidePanel` are local among sync preferences); a tie settles on sync. Two tooltip strings total (`optionsStorageSyncTip`/`optionsStorageLocalTip`, native `title`). ES module self-initializing like `options-proxy.js`; reads only the static `store.syncKeys` list (no `store.ready` needed). Badges are appended to the `<section>`/`<li>` — never inside the `<h2>`, whose `innerText` options.js replaces when labeling — so load order vs the label pass is irrelevant; CSS anchors them absolutely (`.storage-badge` / `.group-storage-badge` / `.row-storage-badge` in `css/options.css`, `:has()`-scoped positioning). The group→keys map (`OPTIONS_STORAGE_GROUPS`) + `classifyGroup`/`applyStorageBadges` are exported pure; groups without settings (backup, the hidden dupes placeholder) stay badge-free. A second `DATA_ROW_BADGES` list marks DATASET rows explicitly (not settings): the favicon gallery link carries an inline crossed-cloud — the vbmFavicon cache is device-local while the Icons group's settings are sync — appended into the row's flex flow (`.storage-badge-inline`) instead of docking at the card edge. Unit-tested by `tests/options-storage-badges.test.js` (drives the real `store.js` key lists + real options.html ids) |
| `src/storage-usage.js` | Storage-usage bar categorization (2026-08 audit extraction; simplified in the 2026-08 storage-audit fix round, docs/review-4.0.8/storage-usage-report.md §15), classic script exposing `window.VBMUsage` (`isIconKey`/`categorize`), loaded by `pages/options.html` before the classic `src/options.js` (which can't import ESM): the bar visualizes the ICON CACHE against everything else — three segments icon cache / other / free, because the favicon cache (`vbmFaviconIdx` + `vbmFavicon:<host>`) is the only dataset with a dynamic byte budget; "other" is the catch-all, so the bar's totals stay exact when new keys appear. **Rule for new features**: a small state key needs no change here; a NEW LARGE dataset (unbounded cache/journal) must be called out in the census suite's decision table — `tests/storage-usage.test.js` fails until the key is tabled (it scans the real `store.knownKeys` and every `setSetting('…')`/`store.set('…')` literal in `src/`, skipping `store.syncKeys` members, which are out of the local bar's scope). Unit-tested by `tests/storage-usage.test.js` |
| `pages/favicons.html` / `src/favicons.js` / `src/favicon-gallery.js` / `css/favicons.css` | Favicon gallery (4.0.8): the display-only window onto the enrichment cache, reached from the options page's Clear-icon-cache row link (`#favicon-gallery-link`). One card per refreshed host in a `auto-fill minmax(300px)` grid — 24px icon in a 36px slot, host, fetch time + size, and a source badge (直连 direct / 代理 proxy / provider id / legacy dashed) read from the index entry's `src`; below it the host's bookmark rows (title, containing-folder path via tree-render's `buildPathMap`, dead ⚑ with mark time, sync dot for local/unsyncable from the session `vbmSyncStatus` blob — synced stays silent like the tree, `×N` visit pill from `visitStats`; http(s) rows open in a new tab, other schemes stay inert text). Header: live filter input (host/title/url/path), source chips derived from what the cache actually holds (`sourceChips`: All + direct/proxy/each present provider/legacy, with counts), and a compare toggle that swaps every real icon for the `DEFAULT_BOOKMARK_ICON` placeholder it replaced (before/after proof of the service). A muted strip lists the failed-marker hosts with their backoff state (in-window vs gave-up chip); a card whose bookmarks are all gone renders an orphan note. Data assembly is pure in `src/favicon-gallery.js` (`buildGallery`/`filterCards`/`sourceChips`/`sourceKind`/`sourceLabel`/`fmtBytes` — vitest-driven, `tests/favicon-gallery.test.js`); the page shell `src/favicons.js` only does chrome.* IO + escaped innerHTML rendering + events, follows the theme pre-paint like popup.js (plus a live sync/local `theme` onChanged), and re-renders debounced on favicon-cache / deadMarks / visitStats / bookmark / sync-blob changes. Unit-tested by `tests/favicon-gallery.test.js` |
| `src/dead-scan-sw.js` | Dead-scan service-worker runner (v4 task-4 #16): the scan outlives any page. Pages drive it with fire-and-forget `vbm-dead-scan-start|pause|resume|cancel` messages (no sendResponse — state flows back through storage); the runner publishes a throttled live blob to `chrome.storage.local` `vbmDeadScan` (`{ state, done, total, items, results, proxy }`), writes the `deadLastScan` cache on finish, drops the blob on cancel. The marker-PAC proxy session is installed/torn down here (only the permission REQUEST stays page-side). MV3 cold start: `resumeIfNeeded()` at background.js top level resumes a scan from a fresh live blob (the blob doubles as the resume journal); a paused blob stays paused. Every async entry window (start's storage/getTree/gate chain, and the resumeIfNeeded/resume blob reads) is generation-guarded, so a cancel landing mid-read wins and never lets a stale snapshot resurrect the run; the installed PAC is also owned by its generation, so a stale chain's teardown can't strip the proxy session from a newer scan (4.0.8 收尾/低优先级收尾). Started by `src/background.js`. Unit-tested by `tests/dead-scan-sw.test.js` |
| `src/tab-groups-sw.js` / `src/tab-group-utils.js` | Tab-group opener service-worker runner + shared helpers (P3.4 hardening, 5df7631): the open-as-tab-group pipeline moved out of the popup — activating the first new tab closes the popup, which dropped the pending `tabs.create` callbacks, so the group never formed. Pages send fire-and-forget `vbm-tab-group-open-new` { urls, title, color } / `vbm-tab-group-open-into` { urls, groupId } messages and the SW opens + groups. Guards: Chrome too old for `chrome.tabs.group`/`chrome.tabGroups` falls back to a plain batch-open; a vanished group or a closed target window degrades the same way (lastError-checked create retried once without windowId); a failed create is skipped and an empty tabIds never reaches `tabs.group`. utils holds the nine-color palette, the deterministic `pickGroupColor` hash and `cleanGroupTitle` (strips the localized sync suffix off folder display titles). 4.1.0 polish D: `vbm-tab-group-open-new` may carry a `windowId` (closed-record restore) — validated once via `chrome.windows.get`, stale ids degrade to Chrome's default window instead of skipping every create. Unit-tested by `tests/tab-groups-sw.test.js` 2026-08-26: `vbm-tabs-merge-window` { tabIds, groupIds, title, windowId } — the window-head drag merge: moves every tab into the target window, then wraps the non-pinned survivors (`groupIds` — pinned tabs cannot join a group) in one titled group (`mergeTabsAsGroup`). Report round 2 (2026-08-26): `moveTabsToNewWindow` creates the fresh window with `focused:false` — the new window must not steal the foreground from the popup |
| `src/tabgroups-patch.js` | Pure pieces for the tab-groups view's 2026-08-29 title-churn fix: `isTitleOnlyChange(changeInfo)` (every key ∈ {title, favIconUrl, status} → patchable in place), `renderInputsFingerprint(snapshot)` (collision-safe JSON digest of everything view-tabgroups render() reads, field-for-field — non-rendered tab fields deliberately excluded) and `replaceTabRowNode(oldRow, html, env)` (template-built li swap carrying 'dragging'/'drag-over' runtime classes and DOM focus; works on detached fold-stash nodes; null on minimal environments → the caller's next refresh self-heals). Unit-tested by `tests/tabgroups-patch.test.js` |
| `src/risk-banner.js` | Pre-cleanup risk banner (v4 task-4 #14) for the dead/dupes views: per-view copy (`deadRiskBanner`/`dupesRiskBanner`), a help link to Chrome's official backup & restore doc, a × that dismisses for the session and a *Don't show again* ack gated by major+minor (`sameOrNewerMinor` — 4.0.1: patch bumps stay silent, a major or minor bump re-arms it once; parse failure on either side fails OPEN and shows the banner). Pure-ish module with the store/chrome injected; unit-tested by `tests/risk-banner.test.js` |
| `src/announce.js` | Remote announcement layer (4.1.0 §4 decision, built ahead for 4.0.8 — the release-shipping "what's new" is data, not code): the popup/sidepanel fetch `docs/announce.json` from raw.githubusercontent.com (zero keys, zero new service deps) and show a banner. Cache `vbmAnnounce = { ts, etag, data }` TTL 6 h — no network within TTL; pulls happen only on popup/panel opens (≤ 4/day, etag-conditional 304s when quiet), never a poll loop — and the re-pulls are what let a message added to the JSON LATER reach long-installed users (a push channel, not an install-time one-shot). Fetch through the shared GitHub chain (`src/github-source.js`: direct → configured proxy → github.akams.cn mirror top-5) with `If-None-Match` + 4 s timeouts; every failure is silent (offline = no banner, never an error). Targeting — one of three forms per message: `version` (a condition string: exact `"4.0.8"` or comparators `">=4.0.0 <4.1.0"`, parsed by `parseVersionCondition`/`versionSatisfies`; a malformed condition drops the message so a typo never widens the audience), the legacy `minVersion`/`maxVersion` `[min, max)` range, or NO version fields at all (every version — a general push); then ∩ channel (`all`/`popup`/`sidepanel`) ∩ once+not-dismissed; array order = priority. The donation card wins the same frame — the announcement defers (not marked seen). Dismiss records the id in `vbmAnnounceSeen` (cap 100, LRU). Privacy switch `announceEnabled` (default on) — off means zero network. Security: fallback text + labelKey escaped via `htmlspecialchars`, `display` enum (`banner`/`dialog`/`toast`; unknown → dropped), text ≤ 500 chars, messages ≤ 10. `link` accepts a single object or an array (guide + changelog on one banner). The banner renders as a neutral-bordered strip with an accent inline-start bar + a speaker glyph — visually distinct from the warm rose donation card. Pure rules exported for testing; `initAnnounce({ store, $, chrome, _m, channel, donationShowing, openNewTab })` does the fetch + render to `$('announce')`. Wired from `src/neat.js` after the donation card. Unit-tested by `tests/announce.test.js` |
| `src/github-source.js` | Shared GitHub resource fetch chain (4.0.8 announce, built for reuse by future GitHub-resource fetches): direct canonical URL → the user's own proxy when `deadProxyServer` is configured (rides a live dead-scan marker-PAC session, or installs a temporary one and tears it down — `addProxyMarker`/`startProxySession`/`endProxySession`/`proxyControllable` from `src/dead-proxy.js`) → the top-5 github.akams.cn mirror candidates from `src/github-mirrors.js`. Mirrors favicon-enrich's discipline: AbortController timeout per layer (default 4 s), every failure silent, finite candidate list, network-level mirror failures trip that host's 6 h breaker (clean HTTP/validation failures only fail over). `fetchGithubResource({ url, etag, validate, probeUrl, timeoutMs, fetchImpl, chromeImpl, now })` resolves `{ data, etag, source }` / `{ notModified, source }` / null and never throws; the mirror-layer `ensure` call is the ONLY trigger for a node-list refresh. Unit-tested by `tests/github-source.test.js` |
| `src/github-mirrors.js` | GitHub mirror provider management for the announce chain's third layer (4.0.8, reusable): fetches the current node list from github.akams.cn (its homepage ships the list inside a content-hashed Next.js chunk — chunk srcs are discovered from the HTML, then the first chunk containing `{label:"contribute|search",value:…}` is parsed), speed-tests the nodes by fetching `probeUrl` through each mirror (concurrency 6, per-probe 4 s, whole-budget 15 s), and persists the top-5 under `vbmGithubMirrors` (`{ v, ts, refreshedAt, nodes:[{host,latency}], down }`) in chrome.storage.local. Refresh happens ONLY when the fetch chain has already fallen back to mirrors AND the cache is absent / all candidates breaker-tripped / older than 7 d, with refresh attempts rate-limited to one per 24 h; network failures trip a per-host 6 h breaker (same TTL as favicon-enrich). `BUILTIN_MIRRORS` is the measured top-5 seed used on first run or when the refresh cannot run. Pure ESM with injectable fetch/storage/clock; unit-tested by `tests/github-mirrors.test.js` |
| `src/version.js` | Version parsing + comparison (4.0.1, pure ESM — zero chrome/DOM references): `parseVersion` (first three dot-segments, missing → 0), `compareVersions`/`versionBelow`/`versionAtLeast` (full semantic compare — patch-level differences like 4.0 vs 4.0.1 are observable), `sameOrNewerMinor` (the donation card's "patch is silent" gate, so the 4.0 → 4.0.1 upgrade stays a silent patch), `crossedInto(recorded, current, threshold)` (the generic "crossed into version X" banner gate — the 3.x→4.x v4 notice is one user; exact threshold gates stay `crossedInto`'s job). The 4.0.1 rule: a patch bump is silent by default, a major.minor bump may re-arm (`majorOf` was removed in 4.0.1 when the risk banner's ack moved to `sameOrNewerMinor`). Consumed by `src/neat.js` and `src/risk-banner.js`. Unit-tested by `tests/version.test.js` |
| `src/version-info.js` | Version metadata for the palette `/version` command (4.0.8), pure ESM with zero chrome/DOM references: `parseBrowser(ua)` (Edg → OPR → Chrome → Firefox → Safari precedence — Edge ships both tokens) and `collectVersionMeta(...)` → the stable metadata object (app/version/manifestVersion/channel/announce/browser/browserVersion/os/language/userAgent) the VersionDialog renders and copies as JSON; the keys are a public contract (bug reports carry them). The palette collects the live page globals (navigator, manifest, the `vbmAnnounce` cache) and passes them in. Unit-tested by `tests/version-info.test.js` |
| `src/i18n-live.js` | Live UI-language override (4.0.8): Chrome picks the extension locale once from the browser UI language with no runtime switch — the options-page language dropdown (General group) and the palette `/lang <code>` command write `uiLanguage` (store + the `vbmI18nLang`/`vbmI18nDict` localStorage caches), and this classic script — loaded right after store.js in popup/sidepanel/options — patches `chrome.i18n.getMessage`/`getUILanguage` SYNCHRONOUSLY from the cached `_locales/<lang>/messages.json` before any module script renders labels (Chrome-style `$placeholder$` substitution honored, unknown keys fall through to the browser locale). Exposes `window.VBMI18N = { currentLang, selectedLang, supportedLangs, setLang }`; every switch ends in `location.reload()` so there is no live re-render and no load race; `setLang('auto')` (case-insensitive) clears the override; a corrupt dict cache self-heals (keys dropped, no patch); a storage-only `uiLanguage` (options import) is re-applied after `store.ready`, while an existing local override wins the conflict and reconciles storage to it. Known limitations: layout direction stays with the browser UI language (the CSS `__MSG_@@bidi_*__` substitutions are load-time, so ar/he/fa render their strings in LTR layout and the rtl keyboard mirroring stays off), and manifest/SW-side strings (context-menu title, omnibox, command labels) cannot be patched. Unit-tested by `tests/i18n-live.test.js` |
| `src/startup-flags.js` | Startup bookkeeping shared by the popup's transient banners (4.0.4, a42efea — extracted from `src/donation.js`, which imports it, so the logic is testable and a second banner consumer doesn't re-implement it): `applyVersionGate(store, currentVersion)` records `currentVersion` and derives THIS open's flags — `newOrUpgrade` (a fresh install, or a version jump the `sameOrNewerMinor` check treats as an upgrade; patch bumps like 4.0 → 4.0.1 stay silent, a major/minor bump re-arms), `upgradedToV4` (a 3.x → 4.x crossing per `crossedInto` against `V4_THRESHOLD` — it suppresses the `#whats-new` twin on that crossing: the donation card's always-on v4 identity line already owns the 4.0 story) and `upgradedToAnnounced` (a crossing into the current `ANNOUNCED_THRESHOLD` — 4.0.8 introduced the mechanism, 4.1.0 re-arms it — it fires the local `#whats-new` banner exactly once) — and `bumpOpenCount(store)`, the running popup-open counter the donationFactor advances against toward the next ask. Unit-tested by `tests/startup-flags.test.js` |
| `src/dupes.js` | Duplicate-bookmark logic (P3.1, extended in v4 task-2 slice C), pure ESM with zero chrome/DOM references: `normalizeUrl` (lowercase scheme/host via the URL parser, strip hash, drop `utm_*`/`fbclid`/`gclid` params case-insensitively, drop root-only trailing slash, non-http(s) and unparseable URLs pass through for exact-match grouping), `findDupes(bookmarks, { ignoreScheme })` (groups by normalized URL — `ignoreScheme` merges http/https variants — keeps only size>1, items sorted `dateAdded` asc, groups sorted by size desc then key), `pickKeeper(group, strategy, ctx)` (six keep-* strategies: oldest/newest/bookmark-bar/shortest-title/shallowest/most-visited, ctx supplies `inBar`/`depthOf`/`visitCountOf`; most-visited falls back to oldest on all-zero counts), `planDeletion(group, keeper)` = the group minus the keeper. 4.1.0: findDupes memoizes normalizeUrl by the raw URL string (duplicate copies share the exact URL text by definition; URL parsing was the dominant regroup cost, ~70 ms at 6000 bookmarks). Unit-tested by `tests/dupes.test.js` |
| `src/session.js` | Session save (P3.2): `sessionFolderName(date, template)` (local `YYYY-MM-DD HH:mm`, `$date$` placeholder), `tabsToBookmarks(tabs)` (case-insensitive protocol whitelist `/^(https?|ftp|file):/i`, empty title → URL, in-window URL dedup keeping the first), `saveSession({ rootFolderId, folderName, tabs })` (creates the folder then serial `chrome.bookmarks.create`, resolves `{ folderId, count }`, empty list creates nothing). Driven from the palette `/session` command. Unit-tested by `tests/session.test.js` |
| `src/dead-links.js` | Dead-link scan (P3.5, extended in v4 task-2 slice C), zero chrome/DOM dependencies: `checkUrl(url)` (HEAD with timeout, GET retry on 405/501/403, `{status, ok}`; non-http(s) → `skipped`), `checkUrlDual(url, { proxyServer = false, timeoutMs = 8000, signal })` (direct channel first; on failure the user's own proxy server via the marker-PAC (`addProxyMarker`, see dead-proxy.js) gets the final say, yielding `ok`/`dead`/`blocked` — the legacy relay-template channel is retired), `scanBookmarks(items, { concurrency, timeoutMs, checker, onProgress, signal })` → `Map(id → result)` (pooled, abortable with partial results; `checker` defaults to `checkUrl`), plus `filterScannable`/`collectDead`/`statusLabel`/`startDeadScan`/`startPausableScan` helpers. The scan is DRIVEN from the service worker (`src/dead-scan-sw.js`, v4 task-4 #16) so it survives popup/panel closes; `<all_urls>` host permission covers the fetches from either context. Unit-tested by `tests/dead-links.test.js` |
| `src/dead-proxy.js` | Real-proxy support for the dead scan (post-v4): routes the scan's second channel through the user's OWN HTTP/HTTPS/SOCKS5 proxy — the extension never implements one. Marker-PAC mechanism: `startProxySession` installs a PAC routing only URLs tagged with `PROXY_MARKER` (`__vbm_px=1`, appended by `addProxyMarker`) through the proxy, everything else DIRECT — so direct and proxied probes run concurrently in the pool and no other tab's traffic is touched (caveat: a system-level proxy is not inherited during the scan window; no `; DIRECT` fallback in the PAC, so a dead proxy can never degrade silently). Pure parts `parseProxyServer` (rejects credentials/paths/bad ports), `formatProxyServer`, `buildPacScript`; chrome wrappers `proxyPermission`/`requestProxyPermission` (the `proxy` permission is install-time required — Chrome refuses it as optional — so contains() is the real gate and request() a never-prompting fallback), `proxyControllable` (levelOfControl — another extension owning proxy settings degrades the scan to direct-only), `startProxySession`/`endProxySession` (clear removes only this extension's settings), `testProxyReachable` (any HTTP response through the PAC counts; the dead view persists only a reachable server as `deadProxyServer`). Unit-tested by `tests/dead-proxy.test.js` |
| `src/search.js` | Popup search (P1 module): owns `searchMode`, the lazily rebuilt flat fuzzy index (`window.VBMFuzzy.rank`), `<mark>` result rendering, saved-query restore, the custom `#search-clear` button (the native webkit cancel glyph is hidden in CSS — unreliable appearance, tiny target; v4 task-3 #8: visibility is driven by a `has-query` class on `#search`, updated on every input; 4.0.8: the × click and deleting the query text both persist the wipe UNCONDITIONALLY — after a view switch the box can still hold the query with `searchMode` off, and the mode-gated `quitSearchMode` used to skip the write, restoring the stale query on the next popup open), the search-history zone (arrow ↑/↓/Home/End row nav and → opens the row's context menu, keyboard parity with right-click — v4 task-3 #15; 4.0.1 focus law: the history-area render parks/restores the focused row across the innerHTML swap, with the search input as the empty-list fallback), and all `#search-input` listeners. `initSearch(ctx)` returns `{ input, results, isActive(), quit(ignoreFocus), reset(), updateIndex(tree), refreshPaths() }` (issue #64: `searchShowFolders`, default on, keeps folder rows in the results — dropped BEFORE the 100-row cut when off, so hidden folders never eat the budget; a render that ran before view-manager's path map exists — the boot-order gap, saved-query restore renders from its OWN getTree callback while the map only fills from tree-view's later one — flags `stalePaths`, and neat.js's onTreeGenerated heals it through `refreshPaths()` re-running the current query; rows also pass `meta.pathLabel` (views.pathLabelOf) + `meta.dateAdded` (the index carries it) so the baked tooltip is complete, and folder rows build their title+path+added tooltip through the shared buildRowTooltip); ctx injects `store`, `separatorManager`, `switchBookmarkMenu`, `generateBookmarkHTML`, `highlightTitlePositions`, `rememberState`. No neatools inside. 2026-08-25: `syncHistoryGutter` mirrors `#results`' LIVE scrollbar inset (`offsetWidth−clientWidth`) onto the history area's `padding-inline-end` (after every results render, at history-render entry, and in the resize debounce) — the two zones are separate scrollers, and while the results list overflows its 8px scrollbar otherwise drifts the history × off the results' delete column (overlay-scrollbar systems read 0 and stay untouched). Unit-tested by `tests/search.test.js` (DOM stub + real-module import) 2026-08-26: the idle result bar carries a 暂存全部 plane left of the select entry (one `.search-icon-cluster`, 20px/4px stride — the vertical-axis law) — one click sends every BOOKMARK row of the current results into a staging group NAMED after the query via `ctx.stagingApi.addItemsToNamedGroup`; stands down with the staging master switch |
| `src/fuzzy-core.js` | Fuzzy-search scoring/ranking core (4.0.5 consolidation), pure ESM with zero chrome/DOM references — the fzf-style subsequence matcher the omnibox (`src/search-core.js`'s `rankBookmarks`) and the popup/palette (`src/fuzzy.js` → `window.VBMFuzzy`) share so the two rankers can never diverge. `score(query, text)` → `{ score, positions }` or null (consecutive-run/word-boundary/camelCase bonuses, case-insensitive incl. scripts without case like CJK; positions are char indices into the ORIGINAL string, usable for `<mark>` highlighting — case folding that changes length, e.g. `İ`.toLowerCase() is `i` + U+0307, is remapped back). `rank(query, items)` scores title and URL independently and tiers every hit — 0 exact > 1 prefix > 2 word-start > 3 subsequence — sorting tier asc, then score desc, with `dateAdded` desc only as the final tie-break (a newer looser hit never outranks an exact one); URL scoring strips the structural `https://`/`www.` noise so host starts align, title hits weigh ×2, and url-only matches carry `positions: null` (rendered plain). 4.0.8: `score` gained a span penalty — a tighter subsequence (smaller first→last hit distance) outranks a scattered one when the other bonuses don't decide it. Unit-tested by `tests/fuzzy.test.js` |
| `src/fuzzy.js` | Popup fuzzy-search shim (Phase 2b; 4.0.5: no longer a classic script — the implementation moved to `src/fuzzy-core.js` and this 12-line ES module only re-exposes its `score`/`rank` as `window.VBMFuzzy` for the consumers that read the global synchronously, palette.js / search.js). Loaded as `<script type="module">` by `pages/popup.html` and `pages/sidepanel.html` before `src/neat.js` |
| `src/sort-utils.js` | Folder sorting (Phase 3, issue #33), classic script exposing `window.VBMSort`: pure `sortNodes(nodes, {by, foldersFirst, recursive})` — title order via `Intl.Collator(numeric, base)` or `dateAdded` desc, optional folders-first grouping, recursive returns a deep-copied ordered tree; never mutates the input. 4.0.1 adds the sort-execution helpers (all pure): `snapshotOrder(rootId, children)` (per-level pre-order id snapshot), `planSortMoves(sorted, recursive)`/`planUndoMoves(snapshot, recursive)` (the move plans — a recursive sort's Undo replays EVERY level back to its snapshot, not just the top) and `createSortLock()` (the re-entrancy lock the executor holds so concurrent sorts/undo replays can't interleave). Loaded by `pages/popup.html` before `src/neat.js` (and by `pages/options.html` for the shared `parseSortOptions`) |
| `src/folder-sort.js` | Folder-content sort executor (issue #33; 2026-08 test-audit extraction): the thin `chrome.bookmarks` wrapper that physically reorders a folder's children — the serial move chain over sort-utils.js's pure plan, the re-entrancy lock, and the toast-Undo replay wiring. Unit-tested by `tests/folder-sort.test.js` |
| `src/settings.js` | Option-boolean judgments (2026-08 extraction), pure ESM: the two stored boolean conventions (`'1'`/`''` vs `'true'`/`'false'`) as named predicates mirroring the original inline neat.js expressions exactly, so a switch "going dead" fails a unit test instead of silently no-op'ing. Unit-tested by `tests/settings.test.js` |
| `src/userstyle.js` | Custom-CSS application (v4; 7854f49 hardening): the options page stores user CSS under the `userstyle` key, the popup/side panel inject it as a `<style>` appended to `<body>` so an equal-specificity user rule wins the cascade by source order. Pure module (doc injected) covering both the save and the inject side. Unit-tested by `tests/userstyle.test.js` |
| `src/quick-add.js` | Popup quick-add star (issue #30 + Ctrl/Cmd+D; 2026-08 extraction): bookmarks the active tab into `quickAddFolderId`, or removes it when already saved (mirroring Chrome's native star). neat.js keeps only the `quickAddEnabled` visibility toggle and the click wiring. Unit-tested by `tests/quick-add.test.js` |
| `src/tool-button.js` | Header ⋮ tool button (2026-08 extraction): opens the command palette; hidden when `showToolButton` is off or the palette itself is disabled (opening the palette is its only job). Unit-tested by `tests/tool-button.test.js` |
| `src/wake-up.js` | Command-palette global wake-up (v4 task-2; 2026-08 extraction): consumes the `?palette=1` fallback-window query and the `pendingPaletteOpen` session flag (the `chrome.action.openPopup` path), opens the palette once the popup document reports focus (a 100 ms tick poll with a 50-try/5 s fallback — a cold popup's focus-settling race used to close the palette the instant it opened, 4.0.8), and clears the flag so the next plain open stays clean. Unit-tested by `tests/wake-up.test.js` |
| `src/resize-core.js` | Popup auto-resize decision kernels (2026-08 extraction), pure ESM with zero chrome/DOM references: `decideHeight`/`decideWidthMax`/`clampDragWidth`/`chaseBodyWidth`/`nextZoomLevel` + the drag math — the vitest suites drive the REAL kernels instead of copied ones. `chaseBodyWidth` is the width-drag chase rule: the root carries the pointer target while the body pins to the viewport the native bubble has achieved, until caught up (the 2026-08 resize-smoothness fix for the right-edge blank strip / oscillating right-aligned buttons). Unit-tested by `tests/autoresize.test.js` |
| `src/resize.js` | Popup resize + zoom wiring layer (f8daae2, extracted from neat.js): the three size behaviors sharing one session flag — auto-height (grow on tree interaction, shrink only on initial load, stands down after a manual edge drag — issue #51), the edge-drag resizers (pointer capture survives the pointer leaving the window; screen-edge clamps frozen at pointerdown; the X drag is SHRINK-PACED — the native window follows the document's laid-out width asynchronously and asymmetrically, grow at zero lag / narrow one-commit behind, distance-proportional: GROW takes the pointer target verbatim, SHRINK approaches it at `pacedDragWidth`'s 2.4px/ms cap (bounds the right-edge strip at ~58px; both user-reported failure modes bracket it — the 234px uncapped fling strip and the 660px/s lead-starved "慢动作" that also deadlocked the shrink: the popup sizes from the CONTENT extent, so the document must lay out narrower than the viewport for the window to narrow at all). During the drag `#search` is pinned to the WINDOW edge via `body.width-dragging` + the `#search-ghost` flow placeholder (fixed escapes body's overflow clip; anchors the visible viewport — real-Chrome verified by the lag data, headless anchors the root box so the Docker diag only checks the mechanics), so the right-aligned header buttons hold the visible edge instead of oscillating with the chase; the release takes the full hand target), and the Ctrl/Cmd zoom layer. DOM/chrome shell over resize-core's pure decisions; deps handed in via ctx lazy getters (TDZ-safe). Unit-tested by `tests/resize.test.js`; harness quantifier `scripts/harness/diag/diag-r1-smooth.js` (incl. the PinCheck phase), user-side quantifier `scripts/console/probe-resize.js` v3 |
| `pages/popup.html` / `src/popup.js` / `src/neat.js` | Main popup UI; `pages/sidepanel.html` is a copy of `pages/popup.html` whose `<body>` carries `panel-mode` (keep their script lists in sync — `tests/fuzzy.test.js` asserts parity; `/src/i18n-live.js` loads right after `/src/store.js` in both pages (and options.html) so a language override patches `chrome.i18n` synchronously before any module script renders). All twelve context menus carry `role="menu"`/`role="menuitem"` and the ten dialogs `aria-modal="true"` (4.1.0 added the tab-row/group-head/closed-group/closed-tab menus and the copy-move + bookmark-folder-pick dialogs). staging added the staging group-head menu — the 16th — plus the folder-copy and folder-add submenus, the staging group-assign dialog, and the picker's move/copy buttons/chips/filter). Panel mode: full height, no size restore/auto-resize, resizers hidden. `src/popup.js` restores popup size (and in panel mode maintains the `sidePanelIsOpen` marker + `sidePanelHeartbeat` liveness beat **and holds a `vbm-panel` runtime port whose disconnect tells the SW the panel died** — see `src/panel-behavior.js`); `src/neat.js` (~1000 lines) is the app-shell orchestrator, loaded as an ES module (`<script type="module">`; P1 split complete — separators/dialogs/popup-search/actions/context-menu/keyboard/dnd/tree-render/tree-view/sync-ui extracted to `src/separators.js` / `src/dialogs.js` / `src/search.js` / `src/actions.js` / `src/context-menu.js` / `src/keyboard.js` / `src/dnd.js` / `src/tree-render.js` / `src/tree-view.js` / `src/sync-ui.js`, and `src/neatools.js` is retired). What remains in neat.js: storage-ready bootstrap, i18n labels, RTL/OS/Chrome-version detection, the v4 gentle-ask donation card (`#donation-card` — no countdown, no focus hijack; snooze steps +120/+800 capped at 3200 popup opens, `donationDisabled` opt-out, new installs see the card once as a first-open welcome frame, then the seeded key holds the next ask back ~30 opens). The card's permanent header is the consolidated v4 identity line (`#v4-notice`: "vBookmarks 4.0 — the modern-design remake" + the `guide-v4[.zh].md` link picked by `chrome.i18n.getUILanguage()`, no minor version numbers) and it carries four answers — Donate / Rate it (`#donation-rate` → the CWS listing `STORE_URL`, the same 800-open snooze as a donation) / Later / Don't show again — beside a gradient-heart `#donation-illustration`; the warm rose styling sets it apart from the cool accent-bar announce/whats-new strips, the 4.0.8 remote announcement banner `#announce` (rendered by `src/announce.js` after the donation card — same frame the donation card wins, so the banner defers while the ask shows) and the local what's-new banner `#whats-new` (the network-independent twin, wired in `src/donation.js`: `upgradedToAnnounced` from `applyVersionGate` fires it exactly once on a crossing into the current `ANNOUNCED_THRESHOLD` — 4.0.8 introduced it, 4.1.0 re-arms it — no dismiss, no fetch; a minor-release banner carries only the version summary + the changelog link, the v4 guide lives on the donation card; the remote announce defers while either local banner claims the open), the `highlight-unsynced` body class from the `highlightUnsynced` sync setting, quick-add star (issue #30, `#quick-add-btn` + Ctrl/Cmd+D capture, solid star = already bookmarked → edit dialog; the button hides when `quickAddEnabled` is off, and `#tool-btn` follows `showToolButton` AND `paletteEnabled` — v4 task-3 #20), folder-content sorting (`sortFolderContents`, issue #33 — a thin chrome executor: all planning lives in the pure `VBMSort` functions, a module-level `sortLock` refuses concurrent sorts/undo replays, and the toast Undo replays every level a recursive sort touched), resizers, auto-height (`resetHeight`) — a manual edge drag sets the session-scoped `userResizedHeight` flag and auto-height then stands down for the rest of the session (issue #51: a manually shrunken height used to be re-grown on the next tree click), zoom, Mac/Chrome-536 workarounds, and the module wiring. Popup features living in the modules: fuzzy search over a lazily rebuilt flat index (`VBMFuzzy.rank`, `<mark>` highlight, no-results `.empty-state` row), `(Empty)` rows for childless folders (`.empty-folder`), context menus, dialogs, keyboard nav, drag & drop, separators, sync indicators, and the v4 view system (the seven views — tree/search/tabgroups/recent/stats/dead/dupes — wired in `src/neat.js`; the recent in-tree section of issue #34 became `src/view-recent.js` in v4 task-2 slice B) |
| `pages/options.html` / `src/options.js` / `css/options.css` | Settings page (v4 task-3 #17 merged the former advanced page into it — one page, 21 sections (4.1.1 added the Memory group) (19 visible + 1 hidden placeholder): General (incl. theme, the 4.0.8 `language-select` UI-language dropdown and the `announce-enabled` privacy switch) / Views (the tab-strip display items + every per-view show switch + Enabled/Disabled toggle wired to the `disable*View` keys), then per-view groups in tab order — Tree (`onlyShowBMBar`) / Search (`searchAfterEnter`) / Tab groups (4.1.0: `tabGroupsColorStyle` off/edge/line + `tabGroupsClosedLimit` 5–50) / Recent (`recentCount`) / Stats (`statsEnabled` + clear + search history) / Dead scan (proxy row, strip visibility, concurrency, timeout) / Duplicates (hidden placeholder) — then Icons / Context menus / Tools / Commands (v4 task-4 #6: the custom palette-command manager, driven by the separate ES module `src/options-palette-commands.js`) / Sync / Accessibility / Custom icon / Separators (final polish: split out of Custom styles) / Sorting (4.0.1: the folder-sort `sortOptions` defaults, parsed by the shared `VBMSort.parseSortOptions`) / Custom styles / Backup+reset; the layout is CSS multicol cards `columns: 340px` inside a 1760 px max shell, so 4K widths fill more columns instead of leaving the lower-left blank; 4.0.8: the old footer/thanks block is gone — the GitHub + homepage links and the full version number sit top-right next to the page title (`#header-links` / `#options-version`)). Data-driven settings lists (`generalSettings`, `treeSettings`, `searchSettings`, `viewSettings`, `iconsSettings`, `contextMenuSettings`, `toolsSettings`, `statsSettings`, `syncSettings` arrays) bound to storage. General also carries the `announce-enabled` privacy switch (4.0.8, `announceEnabled` default on — off stops the announcement network fetch). The "Views" group (`optionsGroupViews`): view tabs/path labels/`rememberView` (#6, default on)/`showTabBadges` (#18, default on)/`showStatsView`/`showDeadView`/`showDupesView`; 4.0.8: each `show*View` row also carries an Enabled/Disabled state + toggle button wired to the `disable*View` keys (a disabled view's show checkbox greys out). The "Icons" group (`optionsGroupIcons`): the favicon-enrich switches `faviconEnrich` (default on) / `faviconEnrichAgg` (default on, the aggregator fallback sub-switch greys out when the master is off), `faviconContrast`, a `favicon-cache-clear` button (prefix-collected `vbmFavicon:`/`vbmFaviconIdx` removal; 4.0.8 低优先级收尾: `confirm()`-gated like the other destructive actions and an `optionFaviconCacheEmpty` alert when nothing is cached) — 4.0.8: the row is a flex row whose right-pinned `#favicon-gallery-link` opens `pages/favicons.html`, the display-only gallery of what the enrichment cache holds and a storage-usage bar below it (4.0.8: splits `chrome.storage.local` into segments so the user can judge when the icon cache is worth clearing; 2026-08 storage-audit fix round: simplified to THREE segments — icon cache / other / free — because the favicon cache is the only dataset with a dynamic byte budget, and the hint under the bar now states the scope explicitly: local extension storage only, the ~10MB `chrome.storage.local` quota; the bar carries a `#storage-usage-summary` "used / quota" line above it, per-segment shares in the legend, and each segment `tabindex="0"` so the tooltip value is keyboard-reachable; it measures with `chrome.storage.local.getBytesInUse` when available and debounces burst refreshes 300 ms, refreshing on any local-area change except the transient `vbmDeadScan` live journal). The bar's "other" segment is a deliberate catch-all — the categorization predicates live in `src/storage-usage.js` (see its row) and every unclaimed key lands in "other", so the totals stay exact no matter what keys future features add; only a NEW LARGE dataset (an unbounded cache/journal) needs a segment decision, and `tests/storage-usage.test.js`'s census fails until one is made. Scope: `chrome.storage.local` only — the sync area, `storage.session` and the `vbmI18nDict` locale cache in localStorage (separate origin quota) are intentionally not measured. Destructive options-page actions — `favicon-cache-clear` / `stats-clear` / `import-settings` / `reset-button` — carry the shared `.danger` treatment (same secondary button shape, danger-colored border + text). The "Context menus" group (`optionsGroupContextMenu`): `quickAddContextMenu` (#49, the page right-click entry) and the issue #48 menu-collapse switches `collapseTabGroupMenu` (default off — folder + bookmark menus) / `collapseSortMenu` (default on — folder menu). The "Tools" group (`optionsGroupTools`): the individually switchable v4 chrome `paletteEnabled`/`quickAddEnabled`/`showToolButton` and the one-click classic-experience preset (`optionClassicExperience` unchecks those three plus `showViewTabs` — each feature stays individually re-toggleable in its own section, #20). The "Stats" group (`optionsGroupStats`): `statsEnabled` (+ clear-statistics button) and search history; the Dead-scan group holds `deadScanConcurrency` 1–16 default 4, `deadScanTimeout` 2–30 s default 8 (v4 task-2 §7), the `deadProxyServer` row (4.0.1: a full add / test & save / clear surface driven by `src/options-proxy.js` through the same parse → permission → controllability → reachability gate as the view strip — the `deadProxyTemplate` relay input is retired) and the `dead-proxy-strip-visible` checkbox that re-arms the view's dismissed no-proxy hint; `confirmDeleteFolder` stays in General. The "Backup" group (fourth-round item 12) exports all settings (full `chrome.storage.local` + the `store.syncKeys` sync keys) to a stamped JSON file and imports one back with merge semantics (backup keys overwrite, unmentioned keys kept) after a `confirm()` gate; 4.0.8: the MB-scale favicon cache (`vbmFavicon:*` + `vbmFaviconIdx`) rides along only when the Icons-group `favicon-backup` switch (`faviconBackupInclude`, default on) is on, and is restored in a separate best-effort write so a quota overflow never fails the settings import; 4.0.8 收尾: the settings writes themselves are try/catch-guarded too (a quota/transient failure alerts `settingsImportError` and skips the reload instead of surfacing half-applied settings), and imported icon entries are sanitized before the write (string, `data:image/` prefix, ≤ 96KB — the enricher's MAX_ICON_BYTES; the index key rides along and self-heals on hydrate); 4.0.8 低优先级收尾: export/import always drops the runtime `vbmDeadScan` journal, the download name carries `-with-icons`/`-no-icons`, the header GitHub link points at the project repo and changelog links at `docs/README.md` explicitly, and `#header-since` is a `<span>` instead of a `<p>` inside `<h1>`; the full reset lives there too 4.1.0: the Labs group (optionsGroupLabs, after Backup) carries experimental default-off switches — first member virtualScrollLab (the list views' virtual painter); labsSettings binds like every other checkbox list. Every group header (and a mixed group's differing rows) carries a storage-area badge (cloud = synced / crossed cloud = device-local) from `src/options-storage-badges.js` (see its row). |
| `pages/advanced-options.html` / `src/advanced-options.js` | Redirect stub (v4 task-3 #17): the advanced settings moved into `pages/options.html`; the page's only script does `window.location.replace('options.html')` so old links keep working |
| `vendor/codemirror.js` / `vendor/codemirror.css` | Vendored CodeMirror editor used only by the advanced options page |
| `src/store.js` | Unified storage entry point (`window.store`): in-memory mirror of `chrome.storage.local` with synchronous `get`/`set`/`remove`, one-time idempotent localStorage→chrome.storage migration (`__migrated_v1`), plus an idempotent retired-key cleanup on every load (the `separatorUrl` precedent — 4.0.1 drops the stale `deadProxyTemplate` relay setting the same way, so it stops riding the options-page backup), per-key 200 ms debounced persistence (flushed on `pagehide`; issue #63: the high-churn `scrollTop` key additionally shadows synchronously into localStorage under the dedicated `__scrollTopLS` key — pagehide is not guaranteed on popup close, so the debounced write races teardown; init lets the shadow override the chrome.storage value and reconciles; 4.1.1 分层记忆: four sync-routed sub-layers rememberScroll/rememberOpens/rememberHighlight/rememberSearchQuery (all default '1') refine the master dontRememberState flag — master off wins everything; the highlight layer's two carriers focusID/focusSpot got the SAME localStorage-shadow treatment 4.1.1-rework (`__focusIDLS`/`__focusSpotLS`) — their writes ride the focusin that immediately precedes a popup close, so without the shadow the last "where I was" write died with the page and the reopen showed no highlight), `clearAll()` for reset. **2026-08 perf audit: the init overlay fetches an ENUMERATED key set, never `get(null)`** — `LOCAL_BOOT_KEYS` = KNOWN_KEYS + SYNC_KEYS (local residue feeds the local→sync migration) + `DATA_KEYS` (the boot-read datasets: deadMarks/deadMarkTimes/visitStats/vbmAnnounce/vbmAnnounceSeen/dupesLastResult/deadRiskAck/dupesRiskAck) + `__migrated_v1`/`deadProxyTemplate` — so the MB-scale `vbmFavicon:*` cache and the `vbmDeadScan`/`deadLastScan` blobs stay out of the pre-paint mirror (favicon-enrich.js hydrates its own index; view-dead reads those blobs via direct area gets). A second mirror covers the sync area (500 ms debounce). **2026-08 storage audit (docs/review-4.0.8/storage-usage-report.md §15): access is area-transparent** — `SYNC_KEYS` grew from 5 to ~45 small device-independent preferences (searchHistoryCount joined in 2026-08-26 — a display-count preference like recentCount) (theme/uiLanguage, behavior toggles, view show/disable keys, feature switches, sort/filter prefs; NOT bookmark-id-keyed data like quickAddFolderId/separators*/deadMarks*/visitStats — bookmark ids are device-local — and NOT oversized values like customIcon ~10-14KB > the 8KB/item sync limit, device state, or privacy/local caches), and `store.get`/`set`/`remove`/`adopt` plus the `getSetting`/`setSetting`/`removeSetting` helpers route SYNC_KEYS members to the sync mirror/area automatically, so call sites never name an area. A one-time local→sync migration runs in init (sync area wins; local residue removed only after a successful sync write; localStorage is NOT a migration source — a sync-area deletion must stick, so init drops boot-copy entries the sync area no longer holds instead of resurrecting them). localStorage doubles as the synchronous boot cache for sync-routed keys (popup.js applies the theme pre-paint, i18n-live patches the language synchronously) — `store.set`/`remove`/`adopt`/`setSetting` keep those copies fresh and feature code never writes localStorage directly. `getSyncSetting`/`setSyncSetting` remain as explicit sync-mirror accessors; `store.syncKeys` (read-only) feeds the options-page settings backup, `store.knownKeys` (read-only) the storage-usage census — a new member without a census-table entry fails `tests/storage-usage.test.js`. 4.0.8: `KNOWN_KEYS` gained the `disable*View` keys, and `adopt(key, value)` lets a live `storage.onChanged` listener fold an external write into the mirror without a persist round-trip. Replaces the old `storage.js` |
| `src/sync-engine.js` | Service-worker sync engine (ES module, started by `src/background.js`, P3.6). Walks the bookmark tree, derives per-node `{ indicator, tooltip }` via the pure `computeStatus` (`synced`/`local`/`unsyncable`/unknown `''` from `node.syncing`, Chrome 138+), and publishes the whole map to `chrome.storage.session` key `vbmSyncStatus` (500 ms debounced; the flush skips the rewrite when a sorted-keys fingerprint over id+indicator+tooltip — `ts` excluded — matches the last published map). Refresh triggers: the seven `chrome.bookmarks` event classes (the four id-bearing ones funnel into ONE 300 ms-debounced id-Set pass — a recursive folder sort otherwise serializes one `bookmarks.get` IPC per node, the src/visit-stats-sw.js onMoved pattern — and stay off entirely below Chrome 138), the `vbm-sync-refresh` `chrome.alarms` periodical (from `autoRefreshSync`/`syncRefreshInterval`, clamped to the 0.5 min alarm minimum, and also gated on `showSyncStatus` + Chrome ≥138 — below 138 a timed recompute can only re-derive "unknown"), and the `vbm-sync-refresh` / `vbm-sync-status-request` runtime messages (live on old Chrome — they paint the `unsyncable` markers). The version probe is a minimal in-module `Chrome/<major>` UA parse with fail-open (no SW-side probe existed; version-info.js's parseBrowser is page-side display logic). Unit-tested by `tests/sync-engine.test.js` (chrome API doubles injected on globalThis) |
| `src/sync-manager.js` | Page-side sync-status client (P3.6 rewrite, computation fully moved to `src/sync-engine.js`). Classic-script IIFE that mirrors the `vbmSyncStatus` session blob into memory so renderers read synchronously through `window.syncManager` (`getSyncStatusIndicator`/`getSyncTooltip`/`refreshAllSyncStatus` — surface unchanged); mirror misses send a `vbm-sync-status-request` message, answers arrive via `storage.onChanged` and re-dispatch the `syncStatusChanged` window event. Loaded by `pages/popup.html` and `pages/sidepanel.html`. Unit-tested by `tests/sync-manager-client.test.js` |
| `css/sync-styles.css` | Styles for sync-status indicators. Noise is inverted: `synced` rows render no dot at all, `local` uses the accent token, `unsyncable` the muted token; indicator colors and tooltips are fully token-driven (no per-theme overrides) |
| `css/neat.css` | Popup styles. 4.0.5 visual consistency: the two-line row icon spec (22 px slot / 18 px glyph) gained `#results`-scoped twins plus a panel-mode pair — the ID-level base rule `#results ul li a .favicon-container` always outranked the bare class rules, so the search view's two-line rows kept the single-line spec; the red = delete language now covers every destructive entry (the search-history per-row remove button and its clear-all, the `#remove-separator` / `#search-history-menu-remove` / `#search-history-menu-clear` / `#dupes-group-clean` / `#palette-cmd-delete` menu items) with one unified danger-tint hover (`color-mix(in srgb, var(--vbm-danger) 12%, transparent)` — the dead view's delete-all/delete-selected included; deliberate boundary: the dupes group-level ✓ stays accent because it reads as "apply", not delete); the dead view's start-scan CTA is an accent-filled pill; and the sync dot / dead-mark × corner badges use `inset-inline-end` so they mirror under RTL 4.1.0 perf: `content-visibility: auto` + `contain-intrinsic-size: auto var(--vbm-row-h)` on the heavy list views' rows (tabgroups/dupes/dead/recent/stats heads and rows) — off-screen rows skip layout/paint entirely (diag-41x-perf.js measured ~1.7 s of style/layout per 1371-row tab-groups refresh); focus rows auto-become relevant and render, so keyboard walks are unaffected; #tree stays out (its cold-open path is already optimized and its alignment/connector geometry is sensitive). |
| `assets/icons/` | Images referenced by code/manifest (shipped): `icon.png`, `icon16.png`, `icon32.png`, `icon48.png`, `icon128.png`. Tree folder rows and `javascript:` bookmarklets use inline SVG instead (see `src/icons.js`) |
| `_locales/<lang>/messages.json` | 43 locales (`en` is the baseline, 685 keys); accessed via `chrome.i18n.getMessage()` and `__MSG_*__` in the manifest |
